---
phase: 05-contracts-preflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase5/test_contracts_preflight.py
autonomous: true
requirements:
  - SKEL-01
  - SKEL-02
  - SKEL-03
must_haves:
  truths:
    - "Importer routes annotations into distinct `keypoints_label_<id>` fields by canonical `label_id`."
    - "Invalid visibility values or point/visibility length mismatch block import during preflight."
    - "`summary.mapping` contains required keys (`label_id`, `source_label_name`, `target_field`, `skeleton_labels`, `skeleton_edges`, `visibility_policy`) for each skeleton type."
  artifacts:
    - path: "tests/phase5/test_contracts_preflight.py"
      provides: "Phase 5 RED tests for D-01..D-08 and SKEL-01..03"
      min_lines: 120
  key_links:
    - from: "tests/phase5/test_contracts_preflight.py"
      to: "src/fiftyone_pose_importer/run_import.py"
      via: "import/module reload and behavior assertions"
      pattern: "importlib\\.import_module\\(\"fiftyone_pose_importer\\.run_import\"\\)"
---

<objective>
Define the executable Phase 5 contract suite first (RED) so implementation is constrained by locked user decisions D-01..D-08 and requirements SKEL-01..SKEL-03.

Purpose: Prevent contract drift during importer changes.
Output: New phase5 test module with failing tests covering field identity, visibility preflight, and mapping metadata.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-contracts-preflight/05-CONTEXT.md
@.planning/phases/05-contracts-preflight/05-RESEARCH.md
@src/fiftyone_pose_importer/run_import.py
@tests/phase2/test_pose_mapping_import.py
</context>

## Artifacts this phase produces

- `tests.phase5.test_contracts_preflight._install_fake_fiftyone`
- `tests.phase5.test_contracts_preflight._load_run_import_module`
- `tests.phase5.test_contracts_preflight.test_per_label_id_field_mapping`
- `tests.phase5.test_contracts_preflight.test_no_single_field_collapse`
- `tests.phase5.test_contracts_preflight.test_visibility_invalid_values_fail_preflight`
- `tests.phase5.test_contracts_preflight.test_missing_visibility_defaults_to_two`
- `tests.phase5.test_contracts_preflight.test_mapping_metadata_emitted_with_required_keys`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for ID-based field routing and anti-collapse behavior</name>
  <read_first>
    tests/phase2/test_pose_mapping_import.py
    src/fiftyone_pose_importer/run_import.py
    .planning/phases/05-contracts-preflight/05-CONTEXT.md
  </read_first>
  <files>tests/phase5/test_contracts_preflight.py</files>
  <behavior>
    - Test 1: multi-skeleton input routes annotations to `keypoints_label_<id>` fields per D-01 and D-02.
    - Test 2: no shared fallback field collapse for distinct label_ids per SKEL-02 and D-03.
    - Test 3: label text changes do not change field identity (ID remains canonical) per D-04.
  </behavior>
  <action>Create `tests/phase5/test_contracts_preflight.py` with fake FiftyOne stubs and RED tests asserting `label_id -> keypoints_label_<id>` routing behavior, explicit anti-collapse assertions, and name/slug independence per D-01..D-04. Keep fixture payloads deterministic and aligned to existing test style in `tests/phase2/test_pose_mapping_import.py`.</action>
  <verify>
    <automated>pytest -q tests/phase5/test_contracts_preflight.py::test_per_label_id_field_mapping tests/phase5/test_contracts_preflight.py::test_no_single_field_collapse -x</automated>
  </verify>
  <acceptance_criteria>Both tests exist, fail against current implementation, and explicitly cite `keypoints_label_<id>` target field contract.</acceptance_criteria>
  <done>RED routing contract is established for SKEL-01/SKEL-02 and D-01..D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED tests for visibility preflight contract</name>
  <read_first>
    src/fiftyone_pose_importer/run_import.py
    .planning/phases/05-contracts-preflight/05-CONTEXT.md
    .planning/phases/05-contracts-preflight/05-RESEARCH.md
  </read_first>
  <files>tests/phase5/test_contracts_preflight.py</files>
  <behavior>
    - Test 1: invalid visibility values or point/visibility length mismatch must hard-fail preflight per D-05.
    - Test 2: missing visibility defaults to `2` and increments warning counters per D-06.
  </behavior>
  <action>Extend the phase5 test file with RED tests asserting strict rejection for invalid visibility payloads and explicit default-to-2 with warning rollup for missing visibility, preserving current summary warning paths.</action>
  <verify>
    <automated>pytest -q tests/phase5/test_contracts_preflight.py::test_visibility_invalid_values_fail_preflight tests/phase5/test_contracts_preflight.py::test_missing_visibility_defaults_to_two -x</automated>
  </verify>
  <acceptance_criteria>Visibility tests fail under current routing contract gap but encode D-05/D-06 behavior exactly.</acceptance_criteria>
  <done>RED visibility policy contract is pinned for future implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add RED test for mapping summary metadata schema</name>
  <read_first>
    src/fiftyone_pose_importer/summary.py
    src/fiftyone_pose_importer/run_import.py
    .planning/phases/05-contracts-preflight/05-CONTEXT.md
  </read_first>
  <files>tests/phase5/test_contracts_preflight.py</files>
  <behavior>
    - Test 1: successful import summary includes `mapping` section per D-07.
    - Test 2: each mapping entry contains required keys `label_id`, `source_label_name`, `target_field`, `skeleton_labels`, `skeleton_edges`, `visibility_policy` per D-08 and SKEL-03.
  </behavior>
  <action>Add RED test that validates summary payload schema for the new dedicated mapping artifact and required mapping entry fields. Assert one entry per seen skeleton type and stable `target_field` format.</action>
  <verify>
    <automated>pytest -q tests/phase5/test_contracts_preflight.py::test_mapping_metadata_emitted_with_required_keys -x</automated>
  </verify>
  <acceptance_criteria>Test exists, fails on current implementation, and validates all D-08 mandatory keys in `summary["mapping"]`.</acceptance_criteria>
  <done>RED audit-metadata contract is in place for SKEL-03 and D-07/D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test payload -> importer contract logic | Untrusted synthetic annotations probe validation and mapping boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | visibility parsing in run_import | mitigate | Encode invalid visibility and mismatch payload tests that must fail (D-05) |
| T-05-02 | Tampering | field routing identity | mitigate | Encode tests proving field identity derives from label_id, not mutable names (D-01..D-04) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package install tasks in this plan |
</threat_model>

<verification>
Run `pytest -q tests/phase5/test_contracts_preflight.py -x`; expect RED for new tests before implementation plans.
</verification>

<success_criteria>
- New phase5 contract tests exist and fail for expected reasons.
- D-01..D-08 each have direct test coverage references.
- SKEL-01..SKEL-03 are represented in assertions.
</success_criteria>

<output>
Create `.planning/phases/05-contracts-preflight/05-01-SUMMARY.md` when done
</output>
