---
phase: 05-contracts-preflight
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/fiftyone_pose_importer/run_import.py
  - tests/phase5/test_contracts_preflight.py
autonomous: true
requirements:
  - SKEL-01
  - SKEL-02
must_haves:
  truths:
    - "Importer writes keypoints into separate `keypoints_label_<id>` fields by label_id."
    - "Skeleton rendering metadata is attached per target keypoint field."
    - "Visibility preflight remains strict for invalid data and defaulting semantics remain auditable."
  artifacts:
    - path: "src/fiftyone_pose_importer/run_import.py"
      provides: "Per-label field router and per-field skeleton assignment"
      min_lines: 220
    - path: "tests/phase5/test_contracts_preflight.py"
      provides: "Green tests for routing and visibility contract"
      min_lines: 140
  key_links:
    - from: "run_import.py"
      to: "dataset.skeletons[field_name]"
      via: "per-label skeleton assignment"
      pattern: "dataset\\.skeletons\\[field_name\\]"
    - from: "run_import.py"
      to: "sample[field_name]"
      via: "label_id-routed Keypoints writes"
      pattern: "sample\\[field_name\\]"
---

<objective>
Implement Phase 5 field-routing core so skeleton types are no longer collapsed and visibility semantics remain contract-safe.

Purpose: Deliver deterministic importer behavior for SKEL-01/SKEL-02 using locked decisions D-01..D-06.
Output: Updated importer routing logic and passing phase5 routing/visibility tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-contracts-preflight/05-CONTEXT.md
@.planning/phases/05-contracts-preflight/05-RESEARCH.md
@src/fiftyone_pose_importer/run_import.py
@src/fiftyone_pose_importer/pose_contract.py
@tests/phase5/test_contracts_preflight.py
</context>

## Artifacts this phase produces

- `fiftyone_pose_importer.run_import._target_field_for_label_id`
- `fiftyone_pose_importer.run_import._ensure_dataset_skeleton_field`
- `fiftyone_pose_importer.run_import` per-field keypoint write path (`sample[keypoints_label_<id>]`)
- green behaviors for:
  - `test_per_label_id_field_mapping`
  - `test_no_single_field_collapse`
  - `test_visibility_invalid_values_fail_preflight`
  - `test_missing_visibility_defaults_to_two`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement label_id-based keypoint field routing</name>
  <read_first>
    src/fiftyone_pose_importer/run_import.py
    src/fiftyone_pose_importer/pose_contract.py
    .planning/phases/05-contracts-preflight/05-CONTEXT.md
    tests/phase5/test_contracts_preflight.py
  </read_first>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase5/test_contracts_preflight.py</files>
  <behavior>
    - For each annotation with label_id, write keypoints to `sample["keypoints_label_<label_id>"]` per D-01 and D-02.
    - Different label_ids never merge into one shared keypoint field per SKEL-02 and D-03.
    - Field naming is invariant to label text changes per D-04.
  </behavior>
  <action>Refactor sample write path in `run_import.py` from single `cfg.label_field` sink to per-label bucket writes; route each keypoint to `keypoints_label_<id>` (per D-01/D-02), create `fo.Keypoints` containers lazily per field, and preserve deterministic annotation ordering. Do not keep any single-field fallback path: when annotation `label_id` is missing or invalid, fail preflight explicitly instead of collapsing into a shared field (per D-03/D-04 and SKEL-02).</action>
  <verify>
    <automated>pytest -q tests/phase5/test_contracts_preflight.py::test_per_label_id_field_mapping tests/phase5/test_contracts_preflight.py::test_no_single_field_collapse -x</automated>
  </verify>
  <acceptance_criteria>Routing tests pass; no logic path computes field names from label slugs/names.</acceptance_criteria>
  <done>Importer now creates separate keypoint fields by canonical label_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach per-field skeleton contracts and keep visibility policy strict</name>
  <read_first>
    src/fiftyone_pose_importer/run_import.py
    .planning/phases/05-contracts-preflight/05-RESEARCH.md
    tests/phase5/test_contracts_preflight.py
  </read_first>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase5/test_contracts_preflight.py</files>
  <behavior>
    - `dataset.skeletons[target_field]` is populated with the matching contract labels/edges for each label_id (SKEL-01).
    - Invalid visibility values/length mismatch remains hard-fail preflight per D-05.
    - Missing visibility defaults to `2` with warning counters per D-06.
  </behavior>
  <action>Set `dataset.skeletons` as a field-keyed dict for all resolved label contracts and ensure each routed field has matching `fo.KeypointSkeleton`. Preserve and, where needed, tighten visibility validation paths so invalid vectors always block import (D-05) while missing visibility defaults to 2 with summary warning increments (D-06).</action>
  <verify>
    <automated>pytest -q tests/phase5/test_contracts_preflight.py::test_visibility_invalid_values_fail_preflight tests/phase5/test_contracts_preflight.py::test_missing_visibility_defaults_to_two -x</automated>
  </verify>
  <acceptance_criteria>Per-field skeleton metadata exists and visibility policy tests pass exactly as specified by D-05/D-06.</acceptance_criteria>
  <done>Dataset writes preserve both skeleton rendering and visibility semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datumaro JSON -> importer parser | Untrusted annotation payloads cross into runtime mapping logic |
| importer -> FiftyOne dataset schema | Incorrect field wiring can corrupt downstream visualization semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | label routing function | mitigate | Field key derives only from integer label_id (`keypoints_label_<id>`) |
| T-05-04 | Tampering | visibility parser | mitigate | Keep fail-fast checks for invalid/mismatched visibility (D-05) |
| T-05-05 | Integrity | dataset.skeletons assignment | mitigate | Assign skeleton contracts per field to prevent cross-type edge contamination |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package install tasks in this plan |
</threat_model>

<verification>
- `pytest -q tests/phase5/test_contracts_preflight.py -x`
- `pytest -q tests/phase2/test_pose_mapping_import.py -x` (regression guard)
</verification>

<success_criteria>
- SKEL-01/SKEL-02 behavior is green in phase5 tests.
- D-01..D-06 are implemented with direct assertions.
- Importer no longer collapses multiple skeleton types into one field.
</success_criteria>

<output>
Create `.planning/phases/05-contracts-preflight/05-02-SUMMARY.md` when done
</output>
