---
phase: 05-contracts-preflight
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/fiftyone_pose_importer/run_import.py
  - tests/phase5/test_contracts_preflight.py
  - tests/phase4/test_run_summary_schema.py
autonomous: true
requirements:
  - SKEL-03
must_haves:
  truths:
    - "Run summary contains an auditable mapping section for every skeleton type."
    - "Each mapping entry exposes source-to-target field contract and visibility policy."
    - "Summary schema remains additive and backward-compatible."
  artifacts:
    - path: "src/fiftyone_pose_importer/run_import.py"
      provides: "summary.mapping emission with required keys"
      min_lines: 240
    - path: "tests/phase5/test_contracts_preflight.py"
      provides: "mapping metadata contract checks"
      min_lines: 170
    - path: "tests/phase4/test_run_summary_schema.py"
      provides: "additive summary schema regression for mapping section"
      min_lines: 155
  key_links:
    - from: "run_import.py"
      to: "summary['mapping']"
      via: "post-import metadata assembly"
      pattern: "\"mapping\"\\s*:"
    - from: "summary mapping entries"
      to: "field router"
      via: "target_field == keypoints_label_<id>"
      pattern: "keypoints_label_"
---

<objective>
Deliver the mapping audit artifact required by SKEL-03 and locked decisions D-07/D-08.

Purpose: Make skeleton-type routing inspectable from run outputs.
Output: Summary mapping metadata emission + green contract/regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-contracts-preflight/05-CONTEXT.md
@src/fiftyone_pose_importer/run_import.py
@src/fiftyone_pose_importer/summary.py
@tests/phase5/test_contracts_preflight.py
@tests/phase4/test_run_summary_schema.py
</context>

## Artifacts this phase produces

- `fiftyone_pose_importer.run_import._build_mapping_summary_entries`
- `summary["mapping"]` (dedicated run artifact section)
- mapping entry symbols:
  - `label_id`
  - `source_label_name`
  - `target_field`
  - `skeleton_labels`
  - `skeleton_edges`
  - `visibility_policy`
- green behaviors for:
  - `test_mapping_metadata_emitted_with_required_keys`
  - updated additive summary schema regression in phase4 tests

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit dedicated summary.mapping entries with required contract keys</name>
  <read_first>
    src/fiftyone_pose_importer/run_import.py
    .planning/phases/05-contracts-preflight/05-CONTEXT.md
    tests/phase5/test_contracts_preflight.py
  </read_first>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase5/test_contracts_preflight.py</files>
  <behavior>
    - `summary["mapping"]` exists for successful runs per D-07.
    - Each seen skeleton type contributes one entry with required D-08 keys.
    - `target_field` value matches canonical `keypoints_label_<id>` identity contract.
  </behavior>
  <action>Add explicit mapping assembly in `run_import.py` and persist under `summary["mapping"]` per D-07. Ensure each entry includes exactly the D-08 minimum fields and visibility policy description (`invalid_or_mismatch=fail`, `missing=default_to_2_warn`) consistent with D-05/D-06. Keep entries deterministic (stable order by label_id).</action>
  <verify>
    <automated>pytest -q tests/phase5/test_contracts_preflight.py::test_mapping_metadata_emitted_with_required_keys -x</automated>
  </verify>
  <acceptance_criteria>Phase5 mapping test passes and validates all mandatory D-08 keys with canonical field identity.</acceptance_criteria>
  <done>Import summary now provides auditable skeleton-to-field mapping artifact for SKEL-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve additive summary schema and run focused regression suite</name>
  <read_first>
    tests/phase4/test_run_summary_schema.py
    src/fiftyone_pose_importer/run_import.py
    .planning/phases/05-contracts-preflight/05-VALIDATION.md
  </read_first>
  <files>tests/phase4/test_run_summary_schema.py, tests/phase5/test_contracts_preflight.py</files>
  <behavior>
    - Existing summary keys from phase4 remain present.
    - New `mapping` key is additive and non-breaking.
    - Phase5 and phase4 summary tests pass together.
  </behavior>
  <action>Update phase4 additive summary regression to assert `mapping` presence without removing prior schema keys (`preflight`, `visibility`, `warnings`, `failures`, `summary_path`). Run targeted test matrix to confirm compatibility and SKEL-03 contract closure.</action>
  <verify>
    <automated>pytest -q tests/phase5/test_contracts_preflight.py tests/phase4/test_run_summary_schema.py -x</automated>
  </verify>
  <acceptance_criteria>Both phase5 and phase4 summary tests pass with additive schema guarantees.</acceptance_criteria>
  <done>Mapping metadata is shipped without breaking prior summary consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| importer internal mapping -> run summary artifact | Incorrect metadata could mislead downstream verification and audits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Repudiation | summary.mapping | mitigate | Persist deterministic mapping entries with source and target identifiers (D-07/D-08) |
| T-05-07 | Tampering | mapping field identity | mitigate | Enforce `target_field` from label_id contract, never from mutable names (D-01..D-04) |
| T-05-08 | Integrity | visibility policy description | mitigate | Encode policy text matching executable behavior (D-05/D-06) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package install tasks in this plan |
</threat_model>

<verification>
- `pytest -q tests/phase5/test_contracts_preflight.py -x`
- `pytest -q tests/phase4/test_run_summary_schema.py -x`
- `pytest -q tests/phase5 tests/phase4 -x`
</verification>

<success_criteria>
- SKEL-03 is fully implemented via `summary.mapping`.
- D-07/D-08 are directly observable in run artifacts.
- Summary schema remains additive and backward-compatible.
</success_criteria>

<output>
Create `.planning/phases/05-contracts-preflight/05-03-SUMMARY.md` when done
</output>
