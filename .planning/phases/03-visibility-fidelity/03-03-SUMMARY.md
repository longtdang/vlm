---
phase: 03-visibility-fidelity
plan: 03
status: complete
---

## Accomplishments

- Added canonical manual verification guidance in `README.md` with both entrypoint and `PYTHONPATH=src` module invocation paths.
- Added troubleshooting guidance for module path errors, missing `fiftyone` dependency, and `preflight.schema_mismatches.ambiguous_skeleton`.
- Added explicit verification-config workflow comments in `config.example.yaml` (`cp config.example.yaml local.verify.yaml`).

## Threat Flags

- none
