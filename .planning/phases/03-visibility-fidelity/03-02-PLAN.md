---
phase: 03-visibility-fidelity
plan: 02
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - src/fiftyone_pose_importer/run_import.py
  - src/fiftyone_pose_importer/preflight.py
  - tests/phase2/test_pose_mapping_import.py
autonomous: true
requirements: [VIS-02, VIS-03]
must_haves:
  truths:
    - "Run summary reports visibility-state counts for absent, hidden, and visible keypoints (D-07)."
    - "Run summary reports visibility fallback/default and mismatch diagnostics in machine-readable form (D-07)."
    - "Summary counters are derived from the same mapped visibility path used to write keypoints, preventing drift (D-07)."
    - "Phase 2 deterministic ordering and schema gate behavior remain intact (D-08)."
  artifacts:
    - path: "src/fiftyone_pose_importer/run_import.py"
      provides: "Visibility aggregate counters and summary payload wiring"
    - path: "src/fiftyone_pose_importer/preflight.py"
      provides: "Mismatch/warning surfacing for visibility diagnostics"
    - path: "tests/phase2/test_pose_mapping_import.py"
      provides: "Coverage for summary visibility counters and diagnostics"
  key_links:
    - from: "run_import.py"
      to: "summary['visibility']"
      via: "single-source counter accumulation from mapped visibility arrays"
      pattern: "summary\\[\"visibility\"\\]"
    - from: "run_import.py"
      to: "PreflightReport"
      via: "visibility mismatch/default diagnostics inclusion"
      pattern: "schema_mismatch|preflight"
---

<objective>
Add run-level visibility diagnostics so users can audit visibility fidelity outcomes and fallback behavior after import (D-07, D-08).

Purpose: Make Phase 3 behavior observable at run level, not only per-keypoint.
Output: Summary visibility counters + diagnostics with regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visibility-fidelity/03-CONTEXT.md
@.planning/phases/03-visibility-fidelity/03-RESEARCH.md
@.planning/phases/03-visibility-fidelity/03-01-SUMMARY.md
@src/fiftyone_pose_importer/run_import.py
@src/fiftyone_pose_importer/preflight.py
@tests/phase2/test_pose_mapping_import.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement run-level visibility counters from mapped outputs</name>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase2/test_pose_mapping_import.py</files>
  <behavior>
    - Test 1: Import with mixed visibility states reports exact absent/hidden/visible totals.
    - Test 2: Counter values match emitted keypoint metadata (no divergence).
    - Test 3: Failed preflight paths still emit visibility diagnostics payload shape consistently.
  </behavior>
  <action>Introduce a visibility accumulator in `run_import.py` and populate `summary['visibility']` with `absent`, `hidden`, and `visible` totals per D-07. Derive these counters directly from the same mapped visibility arrays used when writing keypoints (research control) to prevent per-keypoint vs summary drift, and preserve existing summary contract keys.</action>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_mapping_import.py -k "visibility_summary_counts" -x</automated>
  </verify>
  <done>Run summaries include deterministic, machine-readable visibility-state totals tied to written outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add default-applied and mismatch diagnostics to summary channel</name>
  <files>src/fiftyone_pose_importer/run_import.py, src/fiftyone_pose_importer/preflight.py, tests/phase2/test_pose_mapping_import.py</files>
  <behavior>
    - Test 1: Missing source visibility increments default-applied diagnostics.
    - Test 2: Invalid visibility length/value increments mismatch diagnostics and blocks writes.
    - Test 3: Existing deterministic order and schema-gate tests remain green.
  </behavior>
  <action>Extend summary diagnostics to include default-applied visibility count and visibility-related mismatch/warning counts per D-07, using existing preflight/reporting patterns. Keep fail-fast behavior for invalid visibility payloads per D-06 and preserve Phase 2 invariants per D-08.</action>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_mapping_import.py -k "visibility_defaulted_diagnostics or preflight_failfast_and_deterministic_pose_order" -x</automated>
  </verify>
  <done>Users can audit both successful visibility preservation and fallback/failure paths directly from summary output.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-keypoint mapping -> run summary aggregation | Internal semantic data crosses into user-facing diagnostic channel |
| Preflight mismatch model -> persisted summary file | Validation outcomes become audit evidence for run correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Repudiation | summary diagnostics | mitigate | Emit explicit visibility counters and default/mismatch diagnostics with stable keys (D-07) |
| T-03-05 | Integrity | counter derivation path | mitigate | Build counters from the same mapped visibility arrays used for keypoint writes to prevent drift |
| T-03-06 | Denial of Service | malformed visibility payloads | mitigate | Preserve fail-fast mismatch handling in preflight/write gate (D-06, D-08) |
</threat_model>

<verification>
- `pytest -q tests/phase2/test_pose_mapping_import.py -k "visibility_summary or visibility_defaulted" -x`
- `pytest -q tests/phase2 -x`
</verification>

<success_criteria>
- Summary includes visibility counts: absent/hidden/visible.
- Summary includes visibility default-applied and mismatch/warning diagnostics.
- Visibility diagnostics remain consistent with emitted keypoint metadata.
- Phase 2 ordering and schema-gate behavior remain unchanged and tested.
</success_criteria>

<output>
Create `.planning/phases/03-visibility-fidelity/03-02-SUMMARY.md` when done
</output>
