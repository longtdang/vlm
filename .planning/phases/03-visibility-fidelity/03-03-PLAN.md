---
phase: 03-visibility-fidelity
plan: 03
type: docs+ops
wave: 3
depends_on: [03-02]
gap_closure: true
files_modified:
  - README.md
  - config.example.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Manual UAT can run with a known-good command path without module-resolution confusion."
    - "Manual UAT instructions include an explicit preflight troubleshooting path for ambiguous skeleton failures."
    - "Gap closure remains additive and does not alter visibility mapping runtime behavior."
  artifacts:
    - path: "README.md"
      provides: "Canonical command path and troubleshooting guidance for manual visibility verification."
    - path: "config.example.yaml"
      provides: "Concrete known-good config baseline guidance for manual verification."
  key_links:
    - from: "README.md"
      to: "src/fiftyone_pose_importer/cli.py"
      via: "Documented module invocation path references actual CLI entrypoint."
      pattern: "python -m fiftyone_pose_importer.cli"
    - from: "README.md"
      to: "config.example.yaml"
      via: "Troubleshooting flow points users to baseline config before rerun."
      pattern: "config.example.yaml"
---

<objective>
Close the UAT verification gap by adding explicit operator guidance and a reliable manual verification path for visibility semantics.

Purpose: prevent false-negative UAT failures caused by command/config setup issues rather than importer logic.
Output: updated docs/config guidance that let users verify hidden vs visible behavior consistently.
</objective>

<context>
@.planning/phases/03-visibility-fidelity/03-UAT.md
@.planning/phases/03-visibility-fidelity/03-01-SUMMARY.md
@.planning/phases/03-visibility-fidelity/03-02-SUMMARY.md
@README.md
@config.example.yaml
</context>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Add canonical verification command guidance</name>
  <files>README.md</files>
  <behavior>
    - User sees one canonical command to run importer and launch FiftyOne.
    - User sees explicit fallback command for local editable/dev invocation.
    - User sees common module-resolution mistakes called out.
  </behavior>
  <action>Update README run instructions to include (a) recommended console entrypoint command, (b) `PYTHONPATH=src python -m fiftyone_pose_importer.cli` fallback, and (c) troubleshooting notes for invalid module path invocation.</action>
  <verify>
    <automated>rg -n "fiftyone-datumaro-import|PYTHONPATH=src python -m fiftyone_pose_importer.cli|ModuleNotFoundError" README.md && PYTHONPATH=src python -m fiftyone_pose_importer.cli --help >/dev/null</automated>
  </verify>
  <done>Manual UAT can be started without command ambiguity.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add preflight troubleshooting guidance for ambiguous skeleton</name>
  <files>README.md, config.example.yaml</files>
  <behavior>
    - User can identify ambiguous skeleton preflight failures from summary output.
    - User gets concrete next-step guidance for selecting/normalizing a canonical skeleton.
    - User has a concrete known-good config baseline (`config.example.yaml`) for visibility verification.
  </behavior>
  <action>Add a short troubleshooting section documenting `preflight.schema_mismatches.ambiguous_skeleton`, what it means, and how to correct source/config input. Clarify expected config structure in `config.example.yaml` comments and include a concrete verification run path (`cp config.example.yaml local.verify.yaml` -> edit paths -> run importer) for repeatable manual checks.</action>
  <verify>
    <automated>rg -n "ambiguous_skeleton|preflight|visibility|local.verify.yaml" README.md config.example.yaml</automated>
  </verify>
  <done>UAT gap from test 2 is reproducibly diagnosable and avoidable in future runs.</done>
</task>
</tasks>

<verification>
- `rg -n "fiftyone-datumaro-import|PYTHONPATH=src python -m fiftyone_pose_importer.cli|ambiguous_skeleton" README.md config.example.yaml`
</verification>

<success_criteria>
- Gap closure plan explicitly addresses UAT Test 2 failure cause.
- Manual verification path is clear and repeatable for visibility checks.
- No runtime behavior changes to visibility mapping logic.
</success_criteria>

<output>
Create `.planning/phases/03-visibility-fidelity/03-03-SUMMARY.md` when done
</output>
