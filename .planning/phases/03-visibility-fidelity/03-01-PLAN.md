---
phase: 03-visibility-fidelity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/fiftyone_pose_importer/run_import.py
  - tests/phase2/test_pose_mapping_import.py
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]
must_haves:
  truths:
    - "Absent keypoints remain non-rendered as [NaN, NaN] while index order is preserved (D-01, D-02)."
    - "Hidden (1) and visible (2) remain distinguishable in imported keypoint metadata (D-01, D-03)."
    - "Original source visibility is preserved alongside normalized visibility for audit (D-04)."
    - "Missing source visibility defaults to 2 and is explicitly marked as default-applied (D-05)."
    - "Invalid visibility values/length still fail fast (D-06)."
  artifacts:
    - path: "src/fiftyone_pose_importer/run_import.py"
      provides: "Visibility fidelity extraction/mapping path with source audit metadata and default marker"
      exports: ["_extract_points_and_visibility", "_normalize_points", "run_import"]
    - path: "tests/phase2/test_pose_mapping_import.py"
      provides: "Regression coverage for absent/hidden/visible semantics and source visibility preservation"
  key_links:
    - from: "run_import.py"
      to: "fo.Keypoint metadata"
      via: "kp['visibility'], kp['source_visibility'], kp['visibility_defaulted']"
      pattern: "source_visibility|visibility_defaulted"
    - from: "run_import.py"
      to: "_normalize_points"
      via: "visibility state controls NaN rendering"
      pattern: "vis == 0"
---

<objective>
Implement visibility-fidelity mapping so imported labels preserve render semantics and source auditability exactly as defined in Phase 3 decisions (D-01..D-06).

Purpose: Deliver trustworthy per-keypoint visibility behavior before adding run-level diagnostics.
Output: Updated conversion logic and tests proving absent/hidden/visible + source/default metadata behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-visibility-fidelity/03-CONTEXT.md
@.planning/phases/03-visibility-fidelity/03-RESEARCH.md
@src/fiftyone_pose_importer/run_import.py
@tests/phase2/test_pose_mapping_import.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit visibility fidelity extraction contract</name>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase2/test_pose_mapping_import.py</files>
  <behavior>
    - Test 1: Annotation with visibility [0,1,2] yields normalized visibility [0,1,2], with vis=0 point as [NaN, NaN], vis=1/2 retaining normalized coordinates.
    - Test 2: Annotation missing visibility defaults to [2..] and records default-applied metadata.
    - Test 3: Invalid visibility values or mismatched visibility length produce fail-fast schema mismatch behavior.
  </behavior>
  <action>Refactor visibility extraction to return normalized visibility plus source visibility and a default-applied marker per D-04 and D-05, while preserving strict validation behavior per D-06. Keep three-state mapping exact per D-01, ensure absent->NaN rendering per D-02, and keep hidden distinct from visible per D-03.</action>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_mapping_import.py -k "visibility_fidelity_mapping" -x</automated>
  </verify>
  <done>Importer produces machine-readable per-keypoint visibility fidelity fields without regressing fail-fast validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist per-keypoint audit metadata in output labels</name>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase2/test_pose_mapping_import.py</files>
  <behavior>
    - Test 1: Each emitted keypoint contains visibility, source visibility, and default marker fields.
    - Test 2: Hidden vs visible annotations remain distinguishable after import.
    - Test 3: Existing deterministic ordering behavior remains unchanged.
  </behavior>
  <action>When creating `fo.Keypoint`, keep `kp['visibility']` for rendering compatibility and add additive machine-readable audit metadata fields (field names chosen per discretion, but explicit) implementing D-04 and D-05. Preserve deterministic annotation ordering and preflight schema-gate behavior per D-08.</action>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_mapping_import.py -k "visibility_metadata_preserved or deterministic_pose_order" -x</automated>
  </verify>
  <done>Imported keypoints are both render-compatible and auditable for source visibility provenance.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datumaro annotation payload -> visibility mapper | Untrusted visibility arrays enter semantic conversion logic |
| Mapper output -> FiftyOne keypoint metadata | Converted + audit fields are persisted and consumed downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `run_import.py` visibility parsing | mitigate | Strict domain/length validation for visibility (0/1/2 only) with fail-fast mismatches (D-06) |
| T-03-02 | Integrity | keypoint rendering semantics | mitigate | Enforce absent->NaN and hidden/visible distinction exactly (D-01, D-02, D-03) |
| T-03-03 | Repudiation | visibility audit metadata | mitigate | Persist source visibility + default-applied marker on emitted keypoints (D-04, D-05) |
</threat_model>

<verification>
- `pytest -q tests/phase2/test_pose_mapping_import.py -k "visibility" -x`
- `pytest -q tests/phase2 -x`
</verification>

<success_criteria>
- VIS-01: absent keypoints are non-rendered (`[NaN, NaN]`) with index preserved.
- VIS-02: hidden and visible keypoints remain distinguishable in output metadata.
- VIS-03: source visibility provenance is preserved with explicit default-applied signaling.
- No regressions in deterministic ordering or fail-fast schema validation.
</success_criteria>

<output>
Create `.planning/phases/03-visibility-fidelity/03-01-SUMMARY.md` when done
</output>
