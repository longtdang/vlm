---
phase: 07-vlm-verification-aggregation
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/fiftyone_pose_importer/run_verify.py
  - tests/phase7/test_run_verify_vlm.py
autonomous: true
requirements:
  - VLM-03

must_haves:
  truths:
    - "run_verify() runs VLM stage when vlm.enabled=true; skips VLM when false (backward compat)"
    - "Deterministic FAIL objects are excluded from VLM evaluation per D-08"
    - "Labels not in VLM config scope are excluded from VLM evaluation per D-06"
    - "VLM artifacts (vlm_report.csv, vlm_report.json, vlm_trace.ndjson) appear in same run_dir as deterministic artifacts per D-19"
    - "summary dict includes vlm_counts and vlm_artifacts keys when vlm_enabled=True"
    - "run_verify() accepts optional _vlm_adapter parameter for test injection (per D-22)"
    - "All existing phase6 tests still pass (no regression)"
  artifacts:
    - path: src/fiftyone_pose_importer/run_verify.py
      provides: "Extended run_verify() with VLM stage, annotation_payloads collection, vlm_artifacts in summary"
      contains: "from .verification.vlm_config import load_vlm_config"
    - path: tests/phase7/test_run_verify_vlm.py
      provides: "Integration tests for full deterministic+VLM pipeline with MockVlmAdapter"
  key_links:
    - from: run_verify.py
      to: verification/vlm_engine.py
      via: "evaluate_vlm_object called per VLM-eligible deterministic PASS result"
      pattern: "evaluate_vlm_object"
    - from: run_verify.py
      to: verification/report_vlm.py
      via: "write_vlm_reports called with same run_root + safe_timestamp"
      pattern: "write_vlm_reports.*run_root.*safe_timestamp"
---

<objective>
Wire the VLM stage into run_verify.py: collect annotation payloads during the deterministic loop,
apply D-08/D-09 gating after the loop, evaluate eligible objects via evaluate_vlm_object, and
write VLM artifacts alongside deterministic reports. Verify with end-to-end integration tests
using MockVlmAdapter so no GPU or real model is needed.

Purpose: Phase 7 is only complete when the pipeline runs top-to-bottom from config → deterministic
loop → VLM stage → three VLM artifact files. This plan closes that loop.

Output:
  - run_verify.py extended (VLM stage integrated after deterministic loop)
  - tests/phase7/test_run_verify_vlm.py (end-to-end integration tests, no GPU)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-vlm-verification-aggregation/07-CONTEXT.md
@src/fiftyone_pose_importer/run_verify.py
@src/fiftyone_pose_importer/verification/vlm_config.py
@src/fiftyone_pose_importer/verification/vlm_engine.py
@src/fiftyone_pose_importer/verification/vlm_client.py
@src/fiftyone_pose_importer/verification/report_vlm.py
@src/fiftyone_pose_importer/verification/types.py
@src/fiftyone_pose_importer/verification/report_csv.py
@tests/phase6/test_run_verify.py
@.planning/phases/07-vlm-verification-aggregation/07-01-SUMMARY.md
@.planning/phases/07-vlm-verification-aggregation/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend run_verify.py with VLM stage integration</name>
  <files>
    src/fiftyone_pose_importer/run_verify.py
  </files>

  <read_first>
    - src/fiftyone_pose_importer/run_verify.py (current full file) — understand exact locations for
      the two changes: (1) annotation_payload collection during deterministic loop and (2) VLM stage
      block after write_run_reports; do NOT remove or change any existing deterministic logic
    - .planning/phases/07-vlm-verification-aggregation/07-RESEARCH.md §Pattern 4 (integration point
      pseudocode) — use this as the blueprint for the VLM block
    - .planning/phases/07-vlm-verification-aggregation/07-CONTEXT.md D-08 (det FAIL skip VLM),
      D-09 (only det PASS in VLM scope proceed), D-01 (model-zoo only — do NOT add httpx or external adapter),
      D-22 (mock injectable via _vlm_adapter param)
    - src/fiftyone_pose_importer/verification/report_vlm.py — write_vlm_reports signature
    - src/fiftyone_pose_importer/verification/vlm_engine.py — evaluate_vlm_object signature
      (requires annotation: dict[str, Any] parameter)
    - src/fiftyone_pose_importer/verification/vlm_client.py — FiftyOneZooAdapter, VlmAdapter Protocol
    - src/fiftyone_pose_importer/verification/report_csv.py — _safe_run_dir (already imported)
  </read_first>

  <behavior>
    - run_verify(config_path, _vlm_adapter=None) signature: existing call sites with one positional arg are unaffected
    - When vlm.enabled=False: no VLM imports invoked, no vlm_counts/vlm_artifacts in summary
    - When vlm.enabled=True and _vlm_adapter=None: FiftyOneZooAdapter instantiated from vlm_config.model_name
    - When vlm.enabled=True and _vlm_adapter=MockVlmAdapter(...): mock used instead (test injection D-22)
    - Deterministic FAIL objects: result.verdict is DeterministicVerdict.FAIL → skipped by VLM loop (D-08)
    - Objects whose label is not in vlm_config → skipped by VLM loop (D-06)
    - crop_path for a VLM-eligible object is readable by PIL — already materialized in deterministic loop
    - crop load failure (PIL.open raises): produces VlmObjectResult with REVIEW and failure_reason "crop_load_error:..."
    - VLM artifacts written to same run_dir as deterministic artifacts (same run_root + safe_timestamp)
    - summary["vlm_enabled"] already set before VLM block; remains True/False
    - summary["vlm_counts"] = {"vlm_total": N, "vlm_pass": P, "vlm_review": R, "vlm_fail": F}
      when vlm_enabled=True
    - summary["vlm_artifacts"] = {"vlm_csv": str, "vlm_json": str, "vlm_ndjson": str}
      when vlm_enabled=True
    - Existing deterministic summary fields (counts, artifacts, objects) unchanged when vlm_enabled=False
    - Existing tests/phase6/ tests all still pass (no API regression)
  </behavior>

  <action>
    Modify src/fiftyone_pose_importer/run_verify.py. Make two targeted changes:

    CHANGE 1 — Add new imports at the top (after existing imports):
      from typing import TYPE_CHECKING
      if TYPE_CHECKING:
          from .verification.vlm_client import VlmAdapter

    Do NOT add unconditional top-level imports of vlm_config, vlm_engine, vlm_client, report_vlm.
    These are imported inside the vlm_enabled block (deferred) to keep the module lightweight
    when VLM is disabled.

    CHANGE 2 — Update function signature:
      def run_verify(config_path: str, _vlm_adapter: "VlmAdapter | None" = None) -> tuple[bool, dict[str, Any]]:
      (Add _vlm_adapter parameter with default None; existing callers pass only config_path → unaffected)

    CHANGE 3 — Collect annotation_payloads during the deterministic loop:
      Before the for-loop over items, initialize:
        annotation_payloads: dict[tuple[str, str], dict[str, Any]] = {}

      Inside the inner loop, after building annotation_payload dict and calling evaluate_object
      (the existing block that appends to results), add:
        annotation_payloads[(sample_id, object_id)] = annotation_payload

      Place this line immediately after the evaluate_object block, inside the same try block,
      before the results.append(engine_outcome.result) call. The annotation_payload variable is
      already in scope at that point (defined earlier in the same loop iteration).

      IMPORTANT: annotation_payloads must be populated for EVERY annotation that reaches
      evaluate_object, not just PASS ones — we only filter in the VLM stage.

    CHANGE 4 — Add VLM stage block after the existing write_run_reports call:
      Location: immediately after this line:
        artifact_paths = write_run_reports(results, run_root=run_root, run_timestamp=safe_timestamp)

      Insert the following block (all imports deferred inside the if-block):

        vlm_results = []
        if vlm_enabled:
            from PIL import Image as PILImage
            from .verification.vlm_config import load_vlm_config, VlmConfigError
            from .verification.vlm_engine import evaluate_vlm_object
            from .verification.vlm_client import FiftyOneZooAdapter
            from .verification.report_vlm import write_vlm_reports as write_vlm_run_reports
            from .verification.vlm_types import VlmObjectResult, VlmVerdict

            vlm_config_raw = (verification_root.get("vlm") or {})
            try:
                vlm_config, vlm_cfg_warnings = load_vlm_config(vlm_config_raw)
                warnings.extend(vlm_cfg_warnings)
            except VlmConfigError as exc:
                warnings.append(f"vlm_config_error:{exc}")
                vlm_config = None

            if vlm_config is not None:
                adapter = _vlm_adapter if _vlm_adapter is not None else FiftyOneZooAdapter(
                    model_name=vlm_config.model_name,
                    max_new_tokens=vlm_config.generation.max_new_tokens,
                )

                for result in results:
                    if result.verdict is not DeterministicVerdict.PASS:
                        continue  # D-08: deterministic FAIL → skip VLM
                    if not vlm_config.is_label_enabled(result.label):
                        continue  # D-06: label not in VLM scope → skip

                    try:
                        crop_img = PILImage.open(result.crop_path).convert("RGB")
                    except Exception as exc:
                        from .verification.vlm_types import VlmVerdict as _VV
                        vlm_results.append(VlmObjectResult(
                            sample_id=result.sample_id,
                            object_id=result.object_id,
                            label=result.label,
                            vlm_status=_VV.REVIEW,
                            object_risk=None,
                            rule_results=[],
                            adapter_model=vlm_config.model_name,
                            failure_reason=f"crop_load_error:{type(exc).__name__}",
                            crop_path=result.crop_path,
                        ))
                        continue

                    annotation = annotation_payloads.get((result.sample_id, result.object_id), {})
                    vlm_outcome = evaluate_vlm_object(
                        result=result,
                        annotation=annotation,
                        crop_image=crop_img,
                        adapter=adapter,
                        vlm_config=vlm_config,
                    )
                    vlm_results.append(vlm_outcome)

                vlm_artifact_paths = write_vlm_run_reports(
                    vlm_results, run_root=run_root, run_timestamp=safe_timestamp
                )

    CHANGE 5 — Update summary dict to include VLM results:
      In the existing summary dict construction block (after the object_records loop),
      add conditional VLM keys:

        if vlm_enabled and vlm_results:
            summary["vlm_counts"] = {
                "vlm_total": len(vlm_results),
                "vlm_pass": sum(1 for r in vlm_results if r.vlm_status.value == "PASS"),
                "vlm_review": sum(1 for r in vlm_results if r.vlm_status.value == "REVIEW"),
                "vlm_fail": sum(1 for r in vlm_results if r.vlm_status.value == "FAIL"),
            }
            summary["vlm_artifacts"] = {k: str(v) for k, v in vlm_artifact_paths.items()}
        elif vlm_enabled:
            # VLM enabled but no eligible objects (e.g., all deterministic FAIL or no labels in scope)
            summary["vlm_counts"] = {"vlm_total": 0, "vlm_pass": 0, "vlm_review": 0, "vlm_fail": 0}
            summary["vlm_artifacts"] = {}

    IMPORTANT CONSTRAINTS:
    - Do NOT remove or restructure any existing deterministic code
    - Do NOT add vlm_results to the existing "objects" list (keep deterministic objects separate)
    - VlmObjectResult and vlm_artifact_paths must only be referenced inside the vlm_enabled block
      to avoid NameError when VLM is disabled
    - The existing `vlm_enabled` variable (already set from verification_root) is reused
  </action>

  <verify>
    <automated>cd /Users/longtdang/KMS/VLM/fiftyone-vlm && python -m pytest tests/phase6/ tests/phase7/ -x -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - python -m pytest tests/phase6/ -q — all existing phase6 tests PASSED (no regression)
    - python -c "from fiftyone_pose_importer.run_verify import run_verify; import inspect; sig = inspect.signature(run_verify); assert '_vlm_adapter' in sig.parameters" exits 0
    - run_verify called with vlm.enabled=False: summary has no "vlm_counts" key
    - run_verify called with vlm.enabled=True and MockVlmAdapter injected:
      summary["vlm_counts"]["vlm_total"] > 0 for datasets with PASS objects in VLM-enabled labels
    - vlm_report.csv, vlm_report.json, vlm_trace.ndjson exist in the run_dir when vlm_enabled=True
  </acceptance_criteria>

  <done>
    run_verify.py extended with VLM stage; all phase6 tests pass; VLM stage produces three
    artifact files when enabled; _vlm_adapter parameter allows mock injection in tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/phase7/test_run_verify_vlm.py — end-to-end integration tests</name>
  <files>
    tests/phase7/test_run_verify_vlm.py
  </files>

  <read_first>
    - tests/phase6/test_run_verify.py — full file; replicate _write_datumaro, _write_config helpers;
      the integration test pattern is: write fixtures to tmp_path, call run_verify, assert on summary
    - src/fiftyone_pose_importer/run_verify.py (just extended in Task 1)
    - src/fiftyone_pose_importer/verification/vlm_client.py — MockVlmAdapter constructor
    - .planning/phases/07-vlm-verification-aggregation/07-CONTEXT.md D-08 (det FAIL skip),
      D-06 (label not in scope skip), D-21 (review_queue ordering)
  </read_first>

  <action>
    Create tests/phase7/test_run_verify_vlm.py:

      Imports:
        from __future__ import annotations
        import json
        from pathlib import Path
        import yaml
        import pytest
        from PIL import Image
        from fiftyone_pose_importer.run_verify import run_verify
        from fiftyone_pose_importer.verification.vlm_client import MockVlmAdapter

      Helper _write_datumaro_vlm(tmp_path) → Path:
        Creates images/sample-1.png (RGB 120x100) in tmp_path.
        Writes datumaro.json with:
          - categories: labels = [{"name": "forklift-with-roll"}, {"name": "forklift-no-roll"}]
          - items = [
              {
                "id": "sample-1",
                "image": {"path": "sample-1.png", "size": [100, 120]},
                "annotations": [
                  {
                    "id": "obj-pass-vlm-scope",   # PASS det, label in VLM scope
                    "type": "bbox", "label_id": 0,
                    "bbox": [10, 10, 30, 20],
                    "attributes": {"clamp_type": "2-arm"},
                    "keypoints": [[10,10],[20,20],[25,20],[30,25]],
                    "visibility": [2,2,2,2],
                  },
                  {
                    "id": "obj-pass-no-vlm-scope",  # PASS det, label NOT in VLM scope
                    "type": "bbox", "label_id": 1,
                    "bbox": [10, 10, 30, 20],
                    "attributes": {"clamp_type": "2-arm"},
                    "keypoints": [[10,10],[20,20],[25,20],[30,25]],
                    "visibility": [2,2,2,2],
                  },
                  {
                    "id": "obj-fail-det",           # FAIL det → should skip VLM
                    "type": "bbox", "label_id": 0,
                    "bbox": [5, 5, 0, 20],          # zero-width bbox → det FAIL
                    "attributes": {"clamp_type": "2-arm"},
                    "keypoints": [[1,1],[2,2],[3,3],[4,4]],
                    "visibility": [2,2,2,2],
                  },
                ],
              }
            ]
        Returns datumaro path.

      Helper _write_vlm_config(tmp_path, datumaro_path, mock_responses=None) → Path:
        Writes verify-vlm.yaml with:
          datumaro_json: str(datumaro_path)
          verification:
            vlm:
              enabled: true
              model_name: "qwen3-vl-2b-instruct-torch"
              thresholds:
                pass_below: 0.20
                review_below: 0.60
              generation:
                max_new_tokens: 256
              labels:
                forklift-with-roll:
                  enabled: true
                  rules:
                    - bbox_localization
                    - bbox_coverage
            image_dir: "images"
            output_dir: str(tmp_path / "runs")
            run_timestamp: "20260614T120000Z"
            deterministic:
              padding_px: 8
              rules:
                global:
                  detection: ["bbox_non_empty"]
                  attribute:
                    - name: required_attributes
                      params:
                        required: ["clamp_type"]
                  skeleton-count:
                    - name: keypoint_count
                      params:
                        expected: 4
                  visibility-format: ["visibility_codes"]
        Returns config path.

      test_vlm_pipeline_produces_three_vlm_artifacts(tmp_path):
        datumaro = _write_datumaro_vlm(tmp_path)
        config = _write_vlm_config(tmp_path, datumaro)
        mock = MockVlmAdapter(default_response='{"error_probability": 0.1, "reason": "ok"}')

        ok, summary = run_verify(str(config), _vlm_adapter=mock)

        assert ok is True
        assert summary["vlm_enabled"] is True
        assert "vlm_artifacts" in summary
        assert Path(summary["vlm_artifacts"]["vlm_csv"]).exists()
        assert Path(summary["vlm_artifacts"]["vlm_json"]).exists()
        assert Path(summary["vlm_artifacts"]["vlm_ndjson"]).exists()

      test_vlm_artifacts_in_same_run_dir_as_deterministic(tmp_path):
        datumaro = _write_datumaro_vlm(tmp_path)
        config = _write_vlm_config(tmp_path, datumaro)
        mock = MockVlmAdapter(default_response='{"error_probability": 0.1, "reason": "ok"}')

        ok, summary = run_verify(str(config), _vlm_adapter=mock)

        det_run_dir = Path(summary["artifacts"]["run_dir"])
        vlm_csv_dir = Path(summary["vlm_artifacts"]["vlm_csv"]).parent
        assert det_run_dir == vlm_csv_dir, (
            f"VLM artifacts not in same run_dir: {vlm_csv_dir} != {det_run_dir}"
        )

      test_deterministic_fail_objects_excluded_from_vlm(tmp_path):
        datumaro = _write_datumaro_vlm(tmp_path)
        config = _write_vlm_config(tmp_path, datumaro)
        mock = MockVlmAdapter(default_response='{"error_probability": 0.1, "reason": "ok"}')

        ok, summary = run_verify(str(config), _vlm_adapter=mock)

        # obj-fail-det has bbox_non_empty FAIL → should NOT appear in VLM results
        vlm_json = json.loads(Path(summary["vlm_artifacts"]["vlm_json"]).read_text())
        vlm_object_ids = {obj["object_id"] for obj in vlm_json["objects"]}
        assert "obj-fail-det" not in vlm_object_ids, "Deterministic FAIL should skip VLM (D-08)"

      test_label_not_in_vlm_scope_excluded(tmp_path):
        datumaro = _write_datumaro_vlm(tmp_path)
        config = _write_vlm_config(tmp_path, datumaro)
        mock = MockVlmAdapter(default_response='{"error_probability": 0.1, "reason": "ok"}')

        ok, summary = run_verify(str(config), _vlm_adapter=mock)

        # obj-pass-no-vlm-scope has label "forklift-no-roll" which is NOT in vlm.labels
        vlm_json = json.loads(Path(summary["vlm_artifacts"]["vlm_json"]).read_text())
        vlm_object_ids = {obj["object_id"] for obj in vlm_json["objects"]}
        assert "obj-pass-no-vlm-scope" not in vlm_object_ids, "Label not in scope should skip VLM (D-06)"

      test_vlm_counts_in_summary(tmp_path):
        datumaro = _write_datumaro_vlm(tmp_path)
        config = _write_vlm_config(tmp_path, datumaro)
        # Mock returns low ep → PASS verdict
        mock = MockVlmAdapter(default_response='{"error_probability": 0.1, "reason": "ok"}')

        ok, summary = run_verify(str(config), _vlm_adapter=mock)

        assert "vlm_counts" in summary
        counts = summary["vlm_counts"]
        # Only obj-pass-vlm-scope is eligible: PASS det + label in VLM scope
        assert counts["vlm_total"] == 1
        assert counts["vlm_pass"] == 1
        assert counts["vlm_review"] == 0
        assert counts["vlm_fail"] == 0

      test_vlm_disabled_produces_no_vlm_artifacts(tmp_path):
        # Reuse phase6 fixture helpers to ensure backward compatibility
        from tests.phase6.test_run_verify import _write_datumaro, _write_config
        datumaro = _write_datumaro(tmp_path)
        config = _write_config(tmp_path, datumaro)  # has vlm.enabled=False

        ok, summary = run_verify(str(config))

        assert ok is True
        assert "vlm_artifacts" not in summary
        assert "vlm_counts" not in summary

      test_vlm_high_ep_produces_fail_verdict(tmp_path):
        datumaro = _write_datumaro_vlm(tmp_path)
        # Use a unique timestamp to avoid collision with other tests
        config_path = tmp_path / "verify-high-ep.yaml"
        config_path.write_text(yaml.safe_dump({
            "datumaro_json": str(datumaro),
            "verification": {
                "vlm": {
                    "enabled": True,
                    "model_name": "qwen3-vl-2b-instruct-torch",
                    "thresholds": {"pass_below": 0.20, "review_below": 0.60},
                    "generation": {"max_new_tokens": 256},
                    "labels": {
                        "forklift-with-roll": {
                            "enabled": True,
                            "rules": ["bbox_localization"],
                        }
                    },
                },
                "image_dir": "images",
                "output_dir": str(tmp_path / "runs"),
                "run_timestamp": "20260614T130000Z",
                "deterministic": {
                    "padding_px": 8,
                    "rules": {
                        "global": {
                            "detection": ["bbox_non_empty"],
                            "attribute": [{"name": "required_attributes", "params": {"required": ["clamp_type"]}}],
                            "skeleton-count": [{"name": "keypoint_count", "params": {"expected": 4}}],
                            "visibility-format": ["visibility_codes"],
                        }
                    },
                },
            },
        }), encoding="utf-8")

        mock = MockVlmAdapter(default_response='{"error_probability": 0.85, "reason": "bad bbox"}')
        ok, summary = run_verify(str(config_path), _vlm_adapter=mock)

        assert ok is True
        assert summary["vlm_counts"]["vlm_fail"] == 1
        assert summary["vlm_counts"]["vlm_pass"] == 0

      test_review_queue_in_vlm_json_is_ordered(tmp_path):
        datumaro = _write_datumaro_vlm(tmp_path)
        config_path = tmp_path / "verify-review-queue.yaml"
        config_path.write_text(yaml.safe_dump({
            "datumaro_json": str(datumaro),
            "verification": {
                "vlm": {
                    "enabled": True,
                    "model_name": "qwen3-vl-2b-instruct-torch",
                    "thresholds": {"pass_below": 0.05, "review_below": 0.50},
                    "generation": {"max_new_tokens": 256},
                    "labels": {
                        "forklift-with-roll": {
                            "enabled": True,
                            "rules": ["bbox_localization"],
                        }
                    },
                },
                "image_dir": "images",
                "output_dir": str(tmp_path / "runs"),
                "run_timestamp": "20260614T140000Z",
                "deterministic": {
                    "padding_px": 8,
                    "rules": {
                        "global": {
                            "detection": ["bbox_non_empty"],
                            "attribute": [{"name": "required_attributes", "params": {"required": ["clamp_type"]}}],
                            "skeleton-count": [{"name": "keypoint_count", "params": {"expected": 4}}],
                            "visibility-format": ["visibility_codes"],
                        }
                    },
                },
            },
        }), encoding="utf-8")

        # ep=0.3 → above pass_below(0.05) → REVIEW; goes into review_queue
        mock = MockVlmAdapter(default_response='{"error_probability": 0.3, "reason": "uncertain"}')
        ok, summary = run_verify(str(config_path), _vlm_adapter=mock)

        vlm_json = json.loads(Path(summary["vlm_artifacts"]["vlm_json"]).read_text())
        # review_queue must exist and be a list
        assert isinstance(vlm_json["review_queue"], list)
  </action>

  <verify>
    <automated>cd /Users/longtdang/KMS/VLM/fiftyone-vlm && python -m pytest tests/phase6/ tests/phase7/ -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - python -m pytest tests/phase6/ -q — all phase6 tests PASSED (no regression)
    - python -m pytest tests/phase7/ -q — all phase7 tests PASSED
    - test_vlm_pipeline_produces_three_vlm_artifacts: vlm_report.csv, vlm_report.json, vlm_trace.ndjson exist
    - test_vlm_artifacts_in_same_run_dir_as_deterministic: parent dir of vlm_csv == run_dir from artifacts
    - test_deterministic_fail_objects_excluded_from_vlm: obj-fail-det not in vlm_json objects
    - test_label_not_in_vlm_scope_excluded: obj-pass-no-vlm-scope not in vlm_json objects
    - test_vlm_disabled_produces_no_vlm_artifacts: no vlm_artifacts/vlm_counts keys in summary
    - Combined test count: python -m pytest tests/phase6/ tests/phase7/ --collect-only -q | grep "test session starts" is clean
  </acceptance_criteria>

  <done>
    test_run_verify_vlm.py created with 8 integration tests; all pass;
    no GPU or real model download; deterministic + VLM pipeline is end-to-end verified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml vlm block → load_vlm_config | User-controlled model_name and label scope |
| ObjectVerificationResult.crop_path → PIL.open | Trusted path from deterministic pipeline opened for VLM |
| _vlm_adapter parameter | Injection point — only used internally/in tests; not exposed via CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Information Disclosure | run_verify summary vlm_artifacts paths | accept | paths are local filesystem; summary is returned to caller only (not written to config-controlled location) |
| T-07-10 | Denial of Service | FiftyOneZooAdapter first-call model download | accept | only triggered with vlm.enabled=true and real FiftyOneZooAdapter; MockVlmAdapter used in CI per D-22 |
| T-07-11 | Tampering | _vlm_adapter injection in run_verify | accept | parameter is not exposed via CLI; only callable from Python; no privilege escalation path |
| T-07-12 | Tampering | annotation_payloads dict key collision | accept | keys are (sample_id, object_id) tuples; both come from the same datumaro annotation; deterministic in the existing loop |
| T-07-SC | Tampering | pip installs | mitigate | no new packages in this plan |
</threat_model>

<verification>
Full regression + integration:
```
cd /Users/longtdang/KMS/VLM/fiftyone-vlm && python -m pytest tests/phase6/ tests/phase7/ -v 2>&1 | tail -50
```
Expected: all phase6 tests pass (no regression) + all phase7 tests pass.

End-to-end smoke (no GPU):
```
cd /Users/longtdang/KMS/VLM/fiftyone-vlm && python -m pytest tests/phase7/test_run_verify_vlm.py -v 2>&1 | tail -30
```
</verification>

<success_criteria>
- run_verify.py extended with VLM stage; annotation_payloads collected during deterministic loop
- D-08: deterministic FAIL objects skipped in VLM loop
- D-06: labels not in VLM scope skipped in VLM loop
- D-19: VLM artifacts written to same run_dir as deterministic artifacts (same run_root + timestamp)
- summary["vlm_counts"] and summary["vlm_artifacts"] present when vlm_enabled=True
- summary has no vlm keys when vlm_enabled=False (backward compat)
- All tests/phase6/ tests pass (no regression from run_verify signature change)
- All tests/phase7/ tests pass (21+ tests across 5 test files, no GPU needed)
</success_criteria>

## Artifacts This Phase Produces

| File | Type | Description |
|------|------|-------------|
| src/fiftyone_pose_importer/run_verify.py | Modified | Extended with VLM stage: annotation_payloads, VLM loop, artifact writing, summary fields |
| tests/phase7/test_run_verify_vlm.py | New | 8 end-to-end integration tests covering VLM pipeline, gating, artifacts, ordering |

<output>
Create `.planning/phases/07-vlm-verification-aggregation/07-03-SUMMARY.md` when done
</output>
