---
phase: 02-pose-contract-mapping
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - src/fiftyone_pose_importer/run_import.py
  - tests/phase2/test_pose_mapping_import.py
autonomous: true
requirements: [POSE-01, POSE-03, OUT-03]
must_haves:
  truths:
    - "Keypoints are mapped in canonical skeleton label order (D-09)"
    - "Missing joints are index-preserved with [NaN, NaN] and visibility=0 (D-10)"
    - "Extra joints, count mismatches, and invalid visibility values are hard failures before writes (D-06, D-07, D-11, D-05)"
    - "Multi-pose ordering is deterministic across runs (D-12)"
  artifacts:
    - path: "src/fiftyone_pose_importer/run_import.py"
      provides: "Deterministic keypoint mapping and strict schema gate"
    - path: "tests/phase2/test_pose_mapping_import.py"
      provides: "Contract tests for ordering/padding/failure behavior"
  key_links:
    - from: "run_import.py"
      to: "pose_contract.extract_canonical_skeleton_contract"
      via: "preflight-before-sample-creation"
      pattern: "preflight"
    - from: "run_import.py"
      to: "fo.Keypoint(points=...)"
      via: "canonical index mapping"
      pattern: "math.nan|visibility"
---

<objective>
Implement deterministic keypoint conversion and strict schema enforcement in run flow so only contract-valid pose labels are written (D-05..D-12).
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pose-contract-mapping/02-CONTEXT.md
@.planning/phases/02-pose-contract-mapping/02-RESEARCH.md
@.planning/phases/02-pose-contract-mapping/02-01-SUMMARY.md
@src/fiftyone_pose_importer/run_import.py
@src/fiftyone_pose_importer/pose_contract.py
@src/fiftyone_pose_importer/preflight.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce canonical keypoint count/order/visibility contract</name>
  <read_first>
    @src/fiftyone_pose_importer/run_import.py
    @src/fiftyone_pose_importer/pose_contract.py
    @.planning/phases/02-pose-contract-mapping/02-CONTEXT.md
  </read_first>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase2/test_pose_mapping_import.py</files>
  <action>Add contract-aware mapping helpers in `run_import.py` that align points to canonical skeleton order per D-09, apply missing-joint padding per D-10, reject extra/count-mismatch payloads per D-06/D-11, and enforce visibility defaults/validation per D-07.</action>
  <acceptance_criteria>All mapping contract cases pass and invalid schemas fail before dataset writes.</acceptance_criteria>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_mapping_import.py -k "canonical_mapping_contract" -x</automated>
  </verify>
  <done>Keypoint conversion is deterministic, schema-safe, and contract-aligned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic multi-pose ordering and preflight gate wiring</name>
  <read_first>
    @src/fiftyone_pose_importer/run_import.py
    @src/fiftyone_pose_importer/preflight.py
  </read_first>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase2/test_pose_mapping_import.py</files>
  <action>Wire Phase 2 contract validation into preflight before sample construction per D-05. Implement deterministic per-item pose ordering preserving Datumaro annotation order, with stable-ID sort when IDs are present per D-12. Emit mismatch diagnostics through PreflightReport categories with expected/actual details per D-06 and aggregated reporting per D-08.</action>
  <acceptance_criteria>Preflight blocks invalid items before writes and repeated runs produce identical pose ordering.</acceptance_criteria>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_mapping_import.py -k "preflight_failfast_and_deterministic_pose_order" -x</automated>
  </verify>
  <done>Run pipeline enforces deterministic pose ordering with fail-fast schema policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply canonical skeleton labels/edges to FiftyOne dataset output (POSE-02)</name>
  <read_first>
    @src/fiftyone_pose_importer/run_import.py
    @src/fiftyone_pose_importer/pose_contract.py
    @.planning/phases/02-pose-contract-mapping/02-CONTEXT.md
  </read_first>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase2/test_pose_mapping_import.py</files>
  <action>Ensure canonical skeleton labels and edges extracted from Datumaro contract are explicitly applied to FiftyOne dataset skeleton metadata during import (e.g., `default_skeleton` or configured skeleton field), and add assertions verifying labels/edges are present and match expected contract in successful import paths.</action>
  <acceptance_criteria>After import, dataset skeleton metadata contains canonical labels and edges matching the validated contract, with automated tests asserting exact structure.</acceptance_criteria>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_mapping_import.py -k "skeleton_labels_edges_applied" -x</automated>
  </verify>
  <done>POSE-02 is explicitly implemented and test-verified in output behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datumaro annotation payloads -> run mapping | Untrusted points/visibility data crosses into label construction |
| preflight gate -> dataset write path | Invalid data must never cross into persisted labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | `run_import.py` keypoint mapping | mitigate | Canonical count/order checks, no silent truncation (D-06, D-09, D-11) |
| T-02-05 | Integrity | visibility semantics | mitigate | Default only when absent; reject invalid length/values (D-07) |
| T-02-06 | Repudiation | mismatch diagnostics | mitigate | expected/actual + aggregated sample IDs in preflight summary (D-06, D-08) |
</threat_model>

## Artifacts this phase produces

- Canonical mapping helpers in `run_import.py`
- Deterministic multi-pose ordering routine in `run_import.py`
- `tests/phase2/test_pose_mapping_import.py`

<verification>
Run:
- `pytest -q tests/phase2/test_pose_mapping_import.py -x`
- `pytest -q tests/phase2 -x`
</verification>

<success_criteria>
Importer writes only contract-valid keypoints with deterministic ordering and explicit fail-fast diagnostics for any schema mismatch.
</success_criteria>

<output>
Create `.planning/phases/02-pose-contract-mapping/02-02-SUMMARY.md` when done
</output>
