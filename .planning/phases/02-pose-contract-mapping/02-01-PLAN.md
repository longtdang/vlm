---
phase: 02-pose-contract-mapping
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/fiftyone_pose_importer/pose_contract.py
  - src/fiftyone_pose_importer/preflight.py
  - tests/phase2/test_pose_contract_preflight.py
autonomous: true
requirements: [POSE-02, OUT-03]
must_haves:
  truths:
    - "Importer rejects missing or ambiguous Datumaro skeleton definitions before any sample writes (D-02, D-04, D-05)"
    - "Importer rejects invalid skeleton edges with explicit diagnostics (D-03)"
    - "Schema mismatch diagnostics are aggregated by type with first-N sample IDs (D-08)"
  artifacts:
    - path: "src/fiftyone_pose_importer/pose_contract.py"
      provides: "Strict Datumaro skeleton contract parsing/validation (D-01..D-04)"
      exports: ["SkeletonContract", "SchemaContractError", "extract_canonical_skeleton_contract"]
    - path: "src/fiftyone_pose_importer/preflight.py"
      provides: "Schema mismatch aggregation model for fail-fast gate (D-05, D-08)"
    - path: "tests/phase2/test_pose_contract_preflight.py"
      provides: "Regression coverage for strict skeleton + preflight diagnostics"
  key_links:
    - from: "run_import.py"
      to: "pose_contract.extract_canonical_skeleton_contract"
      via: "preflight contract gate"
      pattern: "extract_canonical_skeleton_contract"
    - from: "pose_contract.py"
      to: "PreflightReport"
      via: "schema_mismatch emission categories"
      pattern: "schema_mismatch"
---

<objective>
Enforce the strict skeleton/schema preflight contract so corrupted pose mappings are blocked before sample creation (D-01..D-05, D-08).
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pose-contract-mapping/02-CONTEXT.md
@.planning/phases/02-pose-contract-mapping/02-RESEARCH.md
@src/fiftyone_pose_importer/run_import.py
@src/fiftyone_pose_importer/preflight.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create strict Datumaro skeleton contract parser</name>
  <read_first>
    @.planning/phases/02-pose-contract-mapping/02-CONTEXT.md
    @src/fiftyone_pose_importer/run_import.py
  </read_first>
  <files>src/fiftyone_pose_importer/pose_contract.py, tests/phase2/test_pose_contract_preflight.py</files>
  <action>Implement `SkeletonContract`, `SchemaContractError`, and `extract_canonical_skeleton_contract(data)` to parse canonical Datumaro `categories.points` structures per D-01, fail on unusable/missing specs per D-02, reject out-of-range/non-integer joints per D-03, and fail on ambiguous competing specs per D-04.</action>
  <acceptance_criteria>Strict skeleton parsing behavior is test-covered and invalid schema inputs fail with explicit error categories.</acceptance_criteria>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_contract_preflight.py -k "extract_canonical_skeleton_contract" -x</automated>
  </verify>
  <done>Canonical skeleton extraction is deterministic and policy-compliant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend preflight schema mismatch aggregation</name>
  <read_first>
    @src/fiftyone_pose_importer/preflight.py
    @.planning/phases/02-pose-contract-mapping/02-CONTEXT.md
  </read_first>
  <files>src/fiftyone_pose_importer/preflight.py, tests/phase2/test_pose_contract_preflight.py</files>
  <action>Refactor `PreflightReport` to include structured schema mismatch buckets with count and bounded sample IDs (first N) per D-08, and ensure `has_errors()` treats any schema mismatch as blocking per D-05/OUT-03.</action>
  <acceptance_criteria>`preflight` JSON exposes mismatch categories and representative sample IDs, and any mismatch sets `ok=false`.</acceptance_criteria>
  <verify>
    <automated>pytest -q tests/phase2/test_pose_contract_preflight.py -k "preflight_schema_mismatch_aggregation" -x</automated>
  </verify>
  <done>Preflight model supports schema-contract diagnostics required by Phase 2.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datumaro JSON -> importer preflight | Untrusted annotation/category structure enters validation layer |
| preflight diagnostics -> summary output | Error taxonomy is persisted for downstream decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `pose_contract.py` skeleton parsing | mitigate | Strict type/index validation + ambiguity rejection (D-02, D-03, D-04) |
| T-02-02 | Repudiation | `preflight.py` mismatch reporting | mitigate | Category-based aggregated diagnostics with sample IDs (D-08) |
| T-02-03 | Denial of Service | malformed schema propagation | mitigate | Fail-fast preflight gate before writes (D-05, OUT-03) |
</threat_model>

## Artifacts this phase produces

- `SkeletonContract` (`pose_contract.py`)
- `SchemaContractError` (`pose_contract.py`)
- `extract_canonical_skeleton_contract()` (`pose_contract.py`)
- Extended `PreflightReport` schema mismatch API (`preflight.py`)
- `tests/phase2/test_pose_contract_preflight.py`

<verification>
Run: `pytest -q tests/phase2/test_pose_contract_preflight.py -x`
</verification>

<success_criteria>
Strict skeleton schema validation is enforced in preflight with machine-readable mismatch aggregation and deterministic failure semantics.
</success_criteria>

<output>
Create `.planning/phases/02-pose-contract-mapping/02-01-SUMMARY.md` when done
</output>
