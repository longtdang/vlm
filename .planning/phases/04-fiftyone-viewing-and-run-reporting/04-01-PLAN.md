---
phase: 04-fiftyone-viewing-and-run-reporting
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/fiftyone_pose_importer/run_import.py
  - src/fiftyone_pose_importer/summary.py
  - tests/phase4/test_run_summary_schema.py
autonomous: true
requirements: [OUT-02]
must_haves:
  truths:
    - "Summary output includes samples, label counts, warnings, and failures in a stable machine-readable schema."
    - "New summary fields are additive and do not break existing Phase 2/3 keys."
    - "Failure/warning diagnostics include count buckets and detail buckets."
  artifacts:
    - path: "src/fiftyone_pose_importer/run_import.py"
      provides: "Expanded summary contract with additive warning/failure and label-count aggregates."
    - path: "tests/phase4/test_run_summary_schema.py"
      provides: "Regression checks for summary schema shape and additive compatibility."
  key_links:
    - from: "run_import.py"
      to: "summary payload"
      via: "label_counts + warnings + failures blocks added additively"
      pattern: "summary\\[\"label_counts\"\\]|summary\\[\"warnings\"\\]|summary\\[\"failures\"\\]"
---

<objective>
Deliver OUT-02 run-report completeness by adding explicit labels/warnings/failures reporting while keeping existing summary compatibility.

Purpose: make run outcomes auditable without log scraping.
Output: additive summary schema + tests.
</objective>

<context>
@.planning/phases/04-fiftyone-viewing-and-run-reporting/04-CONTEXT.md
@.planning/phases/04-fiftyone-viewing-and-run-reporting/04-RESEARCH.md
@src/fiftyone_pose_importer/run_import.py
@src/fiftyone_pose_importer/summary.py
@tests/phase2/test_pose_mapping_import.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add additive summary schema blocks</name>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase4/test_run_summary_schema.py</files>
  <behavior>
    - Summary contains `label_counts` with keypoint-level totals.
    - Summary contains `warnings` and `failures` count/detail buckets.
    - Existing keys (`preflight`, `visibility`, `written_samples`, etc.) remain present.
  </behavior>
  <action>Extend summary assembly in `run_import.py` to add `label_counts`, `warnings`, and `failures` blocks. Keep all existing top-level fields unchanged and additive-only.</action>
  <verify>
    <automated>pytest -q tests/phase4/test_run_summary_schema.py -k "summary_additive_schema" -x</automated>
  </verify>
  <done>OUT-02 reporting contract includes required run-level aggregates in stable schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire mismatch/default diagnostics into rollups</name>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase4/test_run_summary_schema.py</files>
  <behavior>
    - Warning/failure counts reflect preflight and visibility-default outcomes.
    - Summary detail buckets retain categorized data for debugging.
  </behavior>
  <action>Map existing preflight and visibility data into top-level warning/failure rollups without changing fail-fast gates.</action>
  <verify>
    <automated>pytest -q tests/phase4/test_run_summary_schema.py -k "warning_failure_rollups" -x</automated>
  </verify>
  <done>Summary warnings/failures are actionable and aligned with importer behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve summary path contract on success and failure paths</name>
  <files>src/fiftyone_pose_importer/run_import.py, src/fiftyone_pose_importer/summary.py, tests/phase4/test_run_summary_schema.py</files>
  <behavior>
    - Summary output path remains `<config_stem>.summary.json` adjacent to config file.
    - Failure paths continue to emit machine-readable summary with explicit `summary_path`.
  </behavior>
  <action>Add explicit tests and any wiring updates needed so both success and failure flows preserve D-04 and D-06 summary path guarantees.</action>
  <verify>
    <automated>pytest -q tests/phase4/test_run_summary_schema.py -k "summary_path_contract_success_and_failure" -x</automated>
  </verify>
  <done>Summary location and summary_path contract remain stable through phase-4 schema changes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal diagnostics -> persisted summary file | Runtime validation data is transformed into user-facing machine-readable output |
| Preflight mismatch data -> failure rollups | Misclassification can hide critical run failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Repudiation | summary schema | mitigate | Add explicit warning/failure buckets with stable keys and counts |
| T-04-02 | Integrity | label/warning/failure counters | mitigate | Derive rollups directly from runtime/preflight source data |
| T-04-03 | Tampering | backward compatibility | mitigate | Additive-only schema updates; no key removals/renames |
</threat_model>

<verification>
- `pytest -q tests/phase4/test_run_summary_schema.py -x`
- `pytest -q tests/phase2 tests/phase4 -x`
</verification>

<success_criteria>
- OUT-02 summary includes sample counts, label counts, warnings, and failures.
- Existing summary keys remain intact and backward-compatible.
- Summary rollups are deterministic and categorized.
</success_criteria>

<output>
Create `.planning/phases/04-fiftyone-viewing-and-run-reporting/04-01-SUMMARY.md` when done
</output>
