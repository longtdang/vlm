---
phase: 04-fiftyone-viewing-and-run-reporting
plan: 02
type: tdd
wave: 2
depends_on: [04-01]
files_modified:
  - src/fiftyone_pose_importer/run_import.py
  - src/fiftyone_pose_importer/cli.py
  - tests/phase4/test_launch_behavior.py
  - README.md
autonomous: true
requirements: [OUT-01, OUT-02]
must_haves:
  truths:
    - "FiftyOne launch remains explicit opt-in via `--launch`."
    - "Launch attempts occur only after successful dataset write."
    - "Launch outcome is reflected in summary for OUT-02 observability."
  artifacts:
    - path: "src/fiftyone_pose_importer/cli.py"
      provides: "Stable launch flag wiring and exit-path behavior."
    - path: "src/fiftyone_pose_importer/run_import.py"
      provides: "Launch outcome reporting (`requested/attempted/ok/error`) in summary."
    - path: "tests/phase4/test_launch_behavior.py"
      provides: "Automated launch gating and outcome regression coverage."
    - path: "README.md"
      provides: "User-facing launch/report interpretation guidance."
  key_links:
    - from: "cli.py"
      to: "run_import.py"
      via: "--launch flag passed into launch_app execution path"
      pattern: "run_import\\(args\\.config, launch_app=args\\.launch\\)"
    - from: "run_import.py"
      to: "summary payload"
      via: "launch status block populated from launch attempt outcome"
      pattern: "summary\\[\"launch\"\\]"
---

<objective>
Deliver OUT-01 launch behavior and finalize OUT-02 launch/report observability in the summary contract.

Purpose: make viewing behavior predictable and auditable.
Output: launch semantics + tests + docs.
</objective>

<context>
@.planning/phases/04-fiftyone-viewing-and-run-reporting/04-CONTEXT.md
@.planning/phases/04-fiftyone-viewing-and-run-reporting/04-RESEARCH.md
@.planning/phases/04-fiftyone-viewing-and-run-reporting/04-01-SUMMARY.md
@src/fiftyone_pose_importer/cli.py
@src/fiftyone_pose_importer/run_import.py
@README.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement explicit launch outcome reporting</name>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase4/test_launch_behavior.py</files>
  <behavior>
    - Summary includes launch block with `requested`, `attempted`, `ok`, `error`.
    - Launch not attempted when import preflight fails.
    - Launch attempted only when `launch_app=True` and import write succeeds.
    - Launched dataset retains connected skeleton rendering prerequisites (`default_skeleton` labels/edges present).
    - Launch compatibility stays on `fo.launch_app(dataset)` with no alternate UI dependency path introduced.
  </behavior>
  <action>Extend `run_import` to emit a structured launch status block while preserving existing success/failure returns, fail-fast semantics, and existing `fo.launch_app(dataset)` integration path (no new UI framework dependency).</action>
  <verify>
    <automated>pytest -q tests/phase4/test_launch_behavior.py -k "launch_status_reporting or launch_preserves_connected_skeleton_contract or launch_uses_fo_launch_app" -x</automated>
  </verify>
  <done>OUT-01 behavior is machine-auditable through launch status fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize CLI/README launch contract and regression coverage</name>
  <files>src/fiftyone_pose_importer/cli.py, tests/phase4/test_launch_behavior.py, README.md</files>
  <behavior>
    - CLI `--launch` remains optional and correctly wired.
    - Docs explain when launch occurs and how to interpret launch-related summary output, including failure-path `summary_path`.
  </behavior>
  <action>Add/extend tests for CLI launch flag wiring and update README with launch/report interpretation guidance aligned to summary contract, including explicit summary-path and launch-outcome expectations.</action>
  <verify>
    <automated>pytest -q tests/phase4/test_launch_behavior.py -k "cli_launch_wiring or launch_preflight_guard" -x && rg -n "launch outcome|summary_path|--launch|connected skeleton" README.md</automated>
  </verify>
  <done>OUT-01 and OUT-02 user-facing launch/report behavior is documented and regression-tested.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Import success path -> launch invocation | App launch must not occur in invalid/failure flows |
| Launch exception path -> summary/report output | Launch errors must not erase import diagnostics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Integrity | launch gating | mitigate | Enforce launch-only-after-success guard and test coverage |
| T-04-05 | Repudiation | launch outcome diagnostics | mitigate | Persist structured launch result in summary (`requested/attempted/ok/error`) |
| T-04-06 | Denial of Service | launch exception handling | mitigate | Capture launch errors explicitly without suppressing summary generation |
</threat_model>

<verification>
- `pytest -q tests/phase4/test_launch_behavior.py -x`
- `pytest -q tests/phase2 tests/phase4 -x`
</verification>

<success_criteria>
- OUT-01: dataset viewing launch is explicit, successful-path-only, and test-verified.
- OUT-02: summary includes launch outcome diagnostics in stable schema.
- Launch/report behavior is documented for operators.
</success_criteria>

<output>
Create `.planning/phases/04-fiftyone-viewing-and-run-reporting/04-02-SUMMARY.md` when done
</output>
