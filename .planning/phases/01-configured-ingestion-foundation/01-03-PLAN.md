---
phase: 01-configured-ingestion-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/fiftyone_pose_importer/run_import.py
  - src/fiftyone_pose_importer/summary.py
  - src/fiftyone_pose_importer/cli.py
  - tests/phase1/test_run_flow.py
autonomous: true
requirements: [CONF-03, CONF-04, ING-01, ING-02, ING-03]
must_haves:
  truths:
    - "No dataset writes occur if preflight fails"
    - "Successful runs produce JSON summary and strict exit status policy"
  artifacts:
    - path: "src/fiftyone_pose_importer/run_import.py"
      provides: "Preflight-gated run orchestration"
    - path: "src/fiftyone_pose_importer/summary.py"
      provides: "Machine-readable run summary output"
    - path: "src/fiftyone_pose_importer/cli.py"
      provides: "Exit-code contract"
    - path: "tests/phase1/test_run_flow.py"
      provides: "Integration checks for run behavior"
  key_links:
    - from: "run_import.py"
      to: "preflight.py"
      via: "write gate on preflight pass"
      pattern: "if preflight.*fail"
    - from: "run_import.py"
      to: "summary.py"
      via: "always emit summary"
      pattern: "write_summary"
    - from: "cli.py"
      to: "run_import.py"
      via: "exit status mapping"
      pattern: "return 0|return 1"
---

<objective>
Deliver runnable import orchestration with strict preflight gate, JSON summary output, and strict exit-code behavior.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-configured-ingestion-foundation/01-CONTEXT.md
@.planning/phases/01-configured-ingestion-foundation/01-RESEARCH.md
@.planning/phases/01-configured-ingestion-foundation/01-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement import run orchestration</name>
  <files>src/fiftyone_pose_importer/run_import.py, tests/phase1/test_run_flow.py</files>
  <action>Implement execution pipeline that uses validated config settings, prevents any dataset writes when preflight fails (D-09), and propagates malformed data as full-run failure after report (D-10).</action>
  <verify>
    <automated>pytest -q tests/phase1/test_run_flow.py::test_preflight_failure_blocks_dataset_write tests/phase1/test_run_flow.py::test_successful_preflight_writes_with_configured_dataset_and_label -x</automated>
  </verify>
  <done>Run flow is correctly gated by preflight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement summary artifact and exit policy</name>
  <files>src/fiftyone_pose_importer/summary.py, src/fiftyone_pose_importer/cli.py, tests/phase1/test_run_flow.py</files>
  <action>Always emit machine-readable JSON summary next to config (D-11) and enforce strict exit policy: zero only on successful import; non-zero on any validation/import failure (D-12).</action>
  <verify>
    <automated>pytest -q tests/phase1/test_run_flow.py::test_summary_json_written_next_to_config tests/phase1/test_run_flow.py::test_exit_code_policy_is_strict -x</automated>
  </verify>
  <done>Run output and exit behavior are deterministic and policy-compliant.</done>
</task>
</tasks>

<verification>
Run:
- `pytest -q tests/phase1/test_run_flow.py -x`
- `pytest -q tests/phase1 -x`
</verification>

<success_criteria>
User can run config-driven import with strict diagnostics and failure semantics.
</success_criteria>
