---
phase: 01-configured-ingestion-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fiftyone_pose_importer/config_model.py
  - src/fiftyone_pose_importer/config_loader.py
  - src/fiftyone_pose_importer/contracts.py
  - tests/phase1/test_config.py
  - tests/conftest.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04]
must_haves:
  truths:
    - "User can run config-driven import setup from YAML with required fields"
    - "Invalid keys and invalid local paths fail with explicit preflight errors"
  artifacts:
    - path: "src/fiftyone_pose_importer/config_model.py"
      provides: "Strict config schema and field constraints"
    - path: "src/fiftyone_pose_importer/config_loader.py"
      provides: "YAML loading and config-relative path resolution"
    - path: "tests/phase1/test_config.py"
      provides: "Regression checks for config contract"
  key_links:
    - from: "config_loader.py"
      to: "config_model.py"
      via: "validated config object construction"
      pattern: "model_validate|ValidationError"
    - from: "test_config.py"
      to: "config_loader.py"
      via: "behavior assertions"
      pattern: "load_config"
---

<objective>
Create strict config contracts and YAML loader behavior for Phase 1 preflight.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-configured-ingestion-foundation/01-CONTEXT.md
@.planning/phases/01-configured-ingestion-foundation/01-RESEARCH.md
@.planning/phases/01-configured-ingestion-foundation/01-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define strict config schema</name>
  <files>src/fiftyone_pose_importer/contracts.py, src/fiftyone_pose_importer/config_model.py, tests/phase1/test_config.py</files>
  <action>Implement typed config with required fields (image path, datumaro path, dataset name, label field), forbid unknown keys, and reject non-local path semantics to honor D-01, D-02, and D-04.</action>
  <verify>
    <automated>pytest -q tests/phase1/test_config.py::test_schema_accepts_valid_yaml tests/phase1/test_config.py::test_unknown_fields_fail tests/phase1/test_config.py::test_url_paths_rejected -x</automated>
  </verify>
  <done>Valid YAML config passes while unknown fields and non-local paths fail with clear errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement config loader path resolution</name>
  <files>src/fiftyone_pose_importer/config_loader.py, tests/phase1/test_config.py, tests/conftest.py</files>
  <action>Load YAML safely, resolve relative paths from config directory (D-03), and emit key-specific path validation failures before ingestion begins (CONF-04).</action>
  <verify>
    <automated>pytest -q tests/phase1/test_config.py::test_relative_paths_resolve_from_config_dir tests/phase1/test_config.py::test_invalid_paths_fail_with_keyed_errors -x</automated>
  </verify>
  <done>Config loading is deterministic and preflight-safe.</done>
</task>
</tasks>

<verification>
Run: `pytest -q tests/phase1/test_config.py -x`
</verification>

<success_criteria>
Config-only startup contract is enforced with strict, actionable validation.
</success_criteria>
