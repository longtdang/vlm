---
phase: 01-configured-ingestion-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/fiftyone_pose_importer/image_index.py
  - src/fiftyone_pose_importer/datumaro_reader.py
  - src/fiftyone_pose_importer/matching.py
  - src/fiftyone_pose_importer/preflight.py
  - tests/phase1/test_ingestion_matching.py
autonomous: true
requirements: [ING-01, ING-02, ING-03]
must_haves:
  truths:
    - "Configured images and Datumaro entries are indexed deterministically"
    - "Duplicate or unmatched states are fully reported and fail preflight"
  artifacts:
    - path: "src/fiftyone_pose_importer/matching.py"
      provides: "Deterministic match strategy and duplicate detection"
    - path: "src/fiftyone_pose_importer/preflight.py"
      provides: "Fatal mismatch/malformed gate"
    - path: "tests/phase1/test_ingestion_matching.py"
      provides: "Coverage of ingest matching policy"
  key_links:
    - from: "image_index.py"
      to: "matching.py"
      via: "normalized stem keys"
      pattern: "normalize.*stem"
    - from: "datumaro_reader.py"
      to: "preflight.py"
      via: "malformed record propagation"
      pattern: "malformed|invalid"
---

<objective>
Implement deterministic image/annotation intake and preflight matching policy before write stage.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-configured-ingestion-foundation/01-CONTEXT.md
@.planning/phases/01-configured-ingestion-foundation/01-RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build image and datumaro indexing modules</name>
  <files>src/fiftyone_pose_importer/image_index.py, src/fiftyone_pose_importer/datumaro_reader.py, tests/phase1/test_ingestion_matching.py</files>
  <action>Create deterministic image indexing and Datumaro item loading abstractions for configured sources, including malformed-record capture for later preflight reporting.</action>
  <verify>
    <automated>pytest -q tests/phase1/test_ingestion_matching.py::test_index_images_is_deterministic tests/phase1/test_ingestion_matching.py::test_load_datumaro_items_normalized -x</automated>
  </verify>
  <done>Inputs are loaded into stable internal representations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce matching and mismatch failure policy</name>
  <files>src/fiftyone_pose_importer/matching.py, src/fiftyone_pose_importer/preflight.py, tests/phase1/test_ingestion_matching.py</files>
  <action>Implement normalized basename-stem matching (case-insensitive), duplicate fatal detection, and full mismatch aggregation with fail-after-report semantics (D-05..D-08).</action>
  <verify>
    <automated>pytest -q tests/phase1/test_ingestion_matching.py::test_duplicate_matches_are_fatal tests/phase1/test_ingestion_matching.py::test_reports_all_mismatches_then_fails tests/phase1/test_ingestion_matching.py::test_malformed_keypoints_fail_full_run_preflight -x</automated>
  </verify>
  <done>Preflight blocks invalid match states before write stage.</done>
</task>
</tasks>

<verification>
Run: `pytest -q tests/phase1/test_ingestion_matching.py -x`
</verification>

<success_criteria>
Matching behavior is deterministic, explicit, and fatal on policy violations.
</success_criteria>
