---
phase: 06-deterministic-verification-core-reporting
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - src/fiftyone_pose_importer/run_verify.py
  - src/fiftyone_pose_importer/cli.py
  - pyproject.toml
  - tests/phase6/test_run_verify.py
autonomous: true
requirements: [VER-04, VER-03]
must_haves:
  truths:
    - "Deterministic verification runs fully without VLM dependency (VER-04, D-04)."
    - "CLI exits 0 even when objects fail deterministic checks; failures are in artifacts (D-05)."
    - "Only deterministic PASS objects are marked eligible for optional Phase 7 VLM checks (D-04)."
  artifacts:
    - path: src/fiftyone_pose_importer/run_verify.py
      provides: deterministic verification pipeline orchestrator
    - path: tests/phase6/test_run_verify.py
      provides: deterministic-only integration coverage
  key_links:
    - from: run_verify.py
      to: verification/engine.py + reporting writers
      via: pipeline orchestration
      pattern: "run_deterministic|write_.*report"
---

<objective>
Wire deterministic crop/rules/reporting into executable CLI pipeline with Phase 6 exit-code and gating semantics (D-04, D-05).
Purpose: ship usable deterministic verification command independent of VLM services.
Output: run_verify entrypoint + CLI integration + integration tests.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic run orchestrator</name>
  <read_first>
@src/fiftyone_pose_importer/cli.py
@src/fiftyone_pose_importer/run_import.py
@src/fiftyone_pose_importer/verification/engine.py
@src/fiftyone_pose_importer/verification/report_json.py
  </read_first>
  <files>src/fiftyone_pose_importer/run_verify.py, tests/phase6/test_run_verify.py</files>
  <action>Implement orchestrator to load config, enumerate annotations, run cropper + deterministic rules, then write CSV/JSON/NDJSON artifacts; ensure PASS/FAIL-only deterministic outputs (D-01) with strict aggregation (D-02), explicit unevaluable failures (D-03), and per-object `vlm_eligible` true only when deterministic PASS (D-04).</action>
  <acceptance_criteria>Integration tests prove deterministic-only execution path works with VLM disabled/unavailable and still emits complete artifacts.</acceptance_criteria>
  <verify><automated>pytest -q tests/phase6/test_run_verify.py::test_deterministic_only_pipeline_without_vlm -x</automated></verify>
  <done>Run orchestrator executes end-to-end deterministic flow and emits full artifacts without any VLM dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CLI command and exit-code contract</name>
  <read_first>
@src/fiftyone_pose_importer/cli.py
@pyproject.toml
  </read_first>
  <files>src/fiftyone_pose_importer/cli.py, pyproject.toml, tests/phase6/test_run_verify.py</files>
  <action>Add deterministic verify command path (new entrypoint or subcommand) and lock behavior: command returns exit code 0 even when deterministic object failures exist, as long as execution completes and artifacts are written (D-05). Keep non-zero exit only for runtime/config fatal errors.</action>
  <acceptance_criteria>CLI tests verify fail-containing deterministic runs return 0, artifacts contain failures, and fatal startup errors return non-zero.</acceptance_criteria>
  <verify><automated>pytest -q tests/phase6/test_run_verify.py::test_cli_exit_code_zero_with_object_failures -x</automated></verify>
  <done>CLI deterministic verify path is wired and enforces run-complete exit semantics with failure details in reports.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-07 | DoS | pipeline runtime exceptions | mitigate | isolate per-object failures into report rows and continue run |
| T-06-08 | Repudiation | ambiguous run outcomes | mitigate | deterministic artifact emission with run metadata and explicit reasons |
| T-06-SC | Tampering | package installs | accept | no package install task |
</threat_model>

<verification>
Run:
- `pytest -q tests/phase6/test_run_verify.py -x`
- `pytest -q tests/phase6 -x`
</verification>

<success_criteria>
VER-04 is satisfied: deterministic verification is runnable standalone, outputs artifacts, and follows D-04/D-05 gating/exit semantics.
</success_criteria>

## Artifacts this phase produces
- `src/fiftyone_pose_importer/run_verify.py`
- `src/fiftyone_pose_importer/cli.py` (verify command wiring)
- `tests/phase6/test_run_verify.py`
- CLI-accessible deterministic verification entrypoint
