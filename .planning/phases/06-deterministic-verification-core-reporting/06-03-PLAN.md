---
phase: 06-deterministic-verification-core-reporting
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/fiftyone_pose_importer/verification/report_csv.py
  - src/fiftyone_pose_importer/verification/report_json.py
  - src/fiftyone_pose_importer/verification/report_ndjson.py
  - tests/phase6/test_reporting.py
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "Run outputs include CSV + JSON + NDJSON artifacts every run (D-15)."
    - "Exports include final verdict and per-rule details plus crop path and failure reasons (D-16, D-18)."
    - "Artifacts are emitted under unique timestamped run directory (D-17)."
  artifacts:
    - path: src/fiftyone_pose_importer/verification/report_csv.py
      provides: flat triage CSV writer
    - path: src/fiftyone_pose_importer/verification/report_json.py
      provides: structured JSON report writer
    - path: src/fiftyone_pose_importer/verification/report_ndjson.py
      provides: per-event trace writer
  key_links:
    - from: reporting modules
      to: verification result model
      via: shared serialization schema
      pattern: "ObjectVerificationResult"
---

<objective>
Implement deterministic reporting writers for CSV/JSON/NDJSON with consistent schema and timestamped output directory (D-15..D-18).
Purpose: make deterministic verification auditable and machine-consumable.
Output: report writer modules + tests.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement canonical report serialization schema</name>
  <read_first>
@src/fiftyone_pose_importer/summary.py
@src/fiftyone_pose_importer/verification/types.py
  </read_first>
  <files>src/fiftyone_pose_importer/verification/report_json.py, src/fiftyone_pose_importer/verification/report_ndjson.py, tests/phase6/test_reporting.py</files>
  <action>Implement JSON + NDJSON writers from one canonical object-result schema to prevent field drift; include object verdict, per-rule details, crop_path, and deterministic reasons in every record (D-16, D-18); enforce deterministic ordering of records/keys for reproducibility.</action>
  <acceptance_criteria>Tests assert required fields exist in JSON and NDJSON and ordering is stable across repeated runs.</acceptance_criteria>
  <verify><automated>pytest -q tests/phase6/test_reporting.py::test_json_ndjson_schema_and_order -x</automated></verify>
  <done>JSON and NDJSON writers emit stable, schema-aligned deterministic outputs with required verdict/detail/path/reason fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CSV summary writer with run-directory contract</name>
  <read_first>
@src/fiftyone_pose_importer/verification/report_json.py
@.planning/phases/06-deterministic-verification-core-reporting/06-CONTEXT.md
  </read_first>
  <files>src/fiftyone_pose_importer/verification/report_csv.py, tests/phase6/test_reporting.py</files>
  <action>Implement CSV writer using explicit field order and safe cell escaping for spreadsheet consumers; emit CSV/JSON/NDJSON together in timestamped run directory per D-15 and D-17; include verdict/per-rule/crop/reason columns per D-16 and D-18.</action>
  <acceptance_criteria>Tests verify all three files are emitted in one run directory and CSV contains required deterministic columns/values.</acceptance_criteria>
  <verify><automated>pytest -q tests/phase6/test_reporting.py::test_csv_json_ndjson_emitted_with_required_columns -x</automated></verify>
  <done>CSV writer is shipped with deterministic column contract and timestamped run-directory emission parity with JSON/NDJSON outputs.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-05 | Tampering | output file paths | mitigate | enforce run-rooted timestamped directory and reject path traversal tokens |
| T-06-06 | Tampering | CSV formula injection | mitigate | prefix/escape risky leading formula characters in CSV text cells |
| T-06-SC | Tampering | package installs | accept | no package install task |
</threat_model>

<verification>
Run `pytest -q tests/phase6/test_reporting.py -x`
</verification>

<success_criteria>
VER-03 is satisfied with deterministic triage artifacts and stable schemas.
</success_criteria>

## Artifacts this phase produces
- `src/fiftyone_pose_importer/verification/report_csv.py`
- `src/fiftyone_pose_importer/verification/report_json.py`
- `src/fiftyone_pose_importer/verification/report_ndjson.py`
- `tests/phase6/test_reporting.py`
