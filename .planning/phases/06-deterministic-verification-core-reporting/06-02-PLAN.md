---
phase: 06-deterministic-verification-core-reporting
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/fiftyone_pose_importer/verification/cropper.py
  - src/fiftyone_pose_importer/verification/rules.py
  - src/fiftyone_pose_importer/verification/engine.py
  - tests/phase6/test_cropper.py
  - tests/phase6/test_rules_engine.py
autonomous: true
requirements: [VER-01, VER-02]
must_haves:
  truths:
    - "Crop generation follows locked padding/edge policy and invalid bbox behavior (D-10..D-14)."
    - "Deterministic aggregation is any-fail => FAIL, else PASS (D-02), with unevaluable => FAIL (D-03)."
    - "Deterministic outcomes remain PASS/FAIL only (D-01)."
  artifacts:
    - path: src/fiftyone_pose_importer/verification/cropper.py
      provides: deterministic crop generation with skeleton/non-skeleton edge policies
    - path: src/fiftyone_pose_importer/verification/engine.py
      provides: deterministic rule execution + aggregation
  key_links:
    - from: verification/engine.py
      to: verification/rules.py
      via: rule registry dispatch
      pattern: "RULE_REGISTRY|evaluate_"
---

<objective>
Implement deterministic cropper and rule engine core for Phase 6 (D-01..D-14).
Purpose: produce deterministic per-object PASS/FAIL outcomes before reporting.
Output: working cropper + rules + aggregation with unit tests.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build deterministic cropper with locked edge policies</name>
  <read_first>
@.planning/phases/06-deterministic-verification-core-reporting/06-CONTEXT.md
@src/fiftyone_pose_importer/run_import.py
  </read_first>
  <files>src/fiftyone_pose_importer/verification/cropper.py, tests/phase6/test_cropper.py</files>
  <action>Implement fixed-pixel padding cropper (D-10) that consumes validated padding config values from verification config; for skeleton labels preserve padded canvas with out-of-image fill (D-11) and keep out-of-frame points valid as occluded semantics in metadata path (D-12); for non-skeleton labels clip to image bounds (D-13); zero/negative boxes produce object fail reason `invalid_bbox` (D-14).</action>
  <acceptance_criteria>Cropper tests lock skeleton vs non-skeleton behavior, out-of-bound handling, invalid bbox failure reasons, and config-driven padding value application.</acceptance_criteria>
  <verify><automated>pytest -q tests/phase6/test_cropper.py -x</automated></verify>
  <done>Cropper behavior is fully config-driven for fixed padding and validated for skeleton/non-skeleton edge policies via deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic rules registry and aggregation engine</name>
  <read_first>
@src/fiftyone_pose_importer/verification/types.py
@src/fiftyone_pose_importer/verification/config.py
  </read_first>
  <files>src/fiftyone_pose_importer/verification/rules.py, src/fiftyone_pose_importer/verification/engine.py, tests/phase6/test_rules_engine.py</files>
  <action>Implement detection/attribute/skeleton-count/visibility-format deterministic rule evaluators per D-08; apply per-label merged config (D-06..D-07); unknown rule names are warning-only skip (D-09); enforce PASS/FAIL only (D-01), any failed rule => object FAIL (D-02), and any unevaluable/runtime-error rule => FAIL with explicit reason (D-03).</action>
  <acceptance_criteria>Engine tests prove deterministic verdict policy and explicit failure reasons for unevaluable rules, including warning-only behavior for unknown rules.</acceptance_criteria>
  <verify><automated>pytest -q tests/phase6/test_rules_engine.py -x</automated></verify>
  <done>Rules and engine produce deterministic PASS/FAIL-only outcomes with explicit failure-reason coverage for all required rule categories.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-03 | DoS | malformed annotations/rule runtime errors | mitigate | catch unevaluable/runtime errors and convert to deterministic FAIL reason (D-03) |
| T-06-04 | Tampering | bbox and crop bounds inputs | mitigate | numeric validation and invalid_bbox guard path (D-14) |
| T-06-SC | Tampering | package installs | accept | no package install task |
</threat_model>

<verification>
Run `pytest -q tests/phase6/test_cropper.py tests/phase6/test_rules_engine.py -x`
</verification>

<success_criteria>
VER-01 and deterministic-rule portion of VER-02 are satisfied with stable tested behavior.
</success_criteria>

## Artifacts this phase produces
- `src/fiftyone_pose_importer/verification/cropper.py`
- `src/fiftyone_pose_importer/verification/rules.py`
- `src/fiftyone_pose_importer/verification/engine.py`
- `tests/phase6/test_cropper.py`
- `tests/phase6/test_rules_engine.py`
