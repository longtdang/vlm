---
phase: 06-deterministic-verification-core-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fiftyone_pose_importer/verification/__init__.py
  - src/fiftyone_pose_importer/verification/types.py
  - src/fiftyone_pose_importer/verification/config.py
  - tests/phase6/test_verification_types.py
  - tests/phase6/test_verification_config.py
autonomous: true
requirements: [VER-02]
must_haves:
  truths:
    - "Deterministic rule config supports global defaults + per-label overrides (D-06, D-07, D-08)."
    - "Unknown deterministic rule names emit warnings and do not hard-fail the run (D-09)."
  artifacts:
    - path: src/fiftyone_pose_importer/verification/types.py
      provides: Canonical verification result/rule/config data contracts
    - path: src/fiftyone_pose_importer/verification/config.py
      provides: Typed config loader/validator with warning collection
  key_links:
    - from: src/fiftyone_pose_importer/verification/config.py
      to: src/fiftyone_pose_importer/verification/types.py
      via: typed model construction
      pattern: "from \\.types import"
---

<objective>
Define Phase 6 verification contracts and config parsing so all downstream deterministic logic is built on stable interfaces (D-06..D-09).
Purpose: eliminate ambiguity before crop/rule/report implementation.
Output: verification contract + config modules and passing contract tests.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create verification type contracts</name>
  <read_first>
@src/fiftyone_pose_importer/config_model.py
@src/fiftyone_pose_importer/run_import.py
  </read_first>
  <files>src/fiftyone_pose_importer/verification/types.py, src/fiftyone_pose_importer/verification/__init__.py, tests/phase6/test_verification_types.py</files>
  <action>Create typed contracts for deterministic verdicts (PASS/FAIL only per D-01), rule results (rule_name/category/verdict/reason/evaluable), object result payloads (must include crop_path and failure reasons per D-18), and rule-config structures for detection/attribute/skeleton-count/visibility-format categories per D-08.</action>
  <acceptance_criteria>Type tests prove verdict enum excludes REVIEW and result payload requires fields needed by reporting and deterministic gating.</acceptance_criteria>
  <verify><automated>pytest -q tests/phase6/test_verification_types.py -x</automated></verify>
  <done>Verification type contracts are committed and test-covered, with PASS/FAIL-only verdict semantics and required reporting fields enforced by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement verification config parsing with warning semantics</name>
  <read_first>
@src/fiftyone_pose_importer/config_loader.py
@.planning/phases/06-deterministic-verification-core-reporting/06-CONTEXT.md
  </read_first>
  <files>src/fiftyone_pose_importer/verification/config.py, tests/phase6/test_verification_config.py</files>
  <action>Implement loader/normalizer for global defaults + per-label overrides (D-06, D-07), enforce required deterministic categories by default (D-08), and downgrade unknown rule names to warnings not hard failures (D-09). Add explicit fixed-padding policy config fields (for example deterministic crop padding in pixels), validate them, and expose normalized config + warnings collection API for engine consumption.</action>
  <acceptance_criteria>Config tests cover inheritance, overrides, unknown-rule warning path, and explicit config-driven padding policy fields/validation consumed by later plans.</acceptance_criteria>
  <verify><automated>pytest -q tests/phase6/test_verification_config.py -x</automated></verify>
  <done>Config parser exposes validated deterministic padding policy values and merged rule config semantics with warning handling, all guarded by tests.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-01 | Tampering | verification config input | mitigate | strict typed validation + explicit category allowlist |
| T-06-02 | DoS | malformed config payload | mitigate | fail config parse early with actionable errors |
| T-06-SC | Tampering | package installs | accept | no new package installs in this plan |
</threat_model>

<verification>
Run `pytest -q tests/phase6/test_verification_types.py tests/phase6/test_verification_config.py -x`
</verification>

<success_criteria>
Contracts and config parser exist, tests pass, and D-06..D-09 behaviors are locked.
</success_criteria>

## Artifacts this phase produces
- `src/fiftyone_pose_importer/verification/types.py`
- `src/fiftyone_pose_importer/verification/config.py`
- `tests/phase6/test_verification_types.py`
- `tests/phase6/test_verification_config.py`
