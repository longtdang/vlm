---
phase: 06-deterministic-verification-core-reporting
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - src/fiftyone_pose_importer/verification/cropper.py
  - src/fiftyone_pose_importer/run_verify.py
  - tests/phase6/test_cropper.py
  - tests/phase6/test_run_verify.py
autonomous: true
gap_closure: true
requirements: [VER-01]
must_haves:
  truths:
    - "Each verified object has a real crop image artifact on disk (not synthetic path strings) per VER-01."
    - "Skeleton/keypoint crops materialize full padded canvas with out-of-image padded regions per D-11 (with D-10 policy)."
    - "Reports and per-object records point to actual generated crop files per D-18."
  artifacts:
    - path: src/fiftyone_pose_importer/verification/cropper.py
      provides: "Crop materialization API that writes planned crops to disk for skeleton and non-skeleton annotations"
    - path: src/fiftyone_pose_importer/run_verify.py
      provides: "Pipeline wiring from source image -> crop artifact path -> deterministic evaluation/report rows"
    - path: tests/phase6/test_run_verify.py
      provides: "Integration proof that run outputs real crop artifacts and links them in summary/report payloads"
  key_links:
    - from: src/fiftyone_pose_importer/run_verify.py
      to: src/fiftyone_pose_importer/verification/cropper.py
      via: "plan_crop + materialize_crop call path"
      pattern: "plan_crop|materialize_crop"
    - from: src/fiftyone_pose_importer/run_verify.py
      to: verification run artifacts directory
      via: "crop file write and crop_path assignment in ObjectVerificationResult"
      pattern: "crop_path"
---

<objective>
Close Phase 06 verification blocker by replacing synthetic crop paths with real crop artifact generation, including D-11 skeleton padded-canvas materialization, without reworking already verified deterministic rule/report behavior.

Purpose: satisfy VER-01 + D-11 gap from VERIFICATION.md while preserving verified SC2–SC5 logic.
Output: real crop files written per object and linked in deterministic outputs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-deterministic-verification-core-reporting/06-CONTEXT.md
@.planning/phases/06-deterministic-verification-core-reporting/VERIFICATION.md
@.planning/phases/06-deterministic-verification-core-reporting/06-02-SUMMARY.md
@.planning/phases/06-deterministic-verification-core-reporting/06-04-SUMMARY.md
@src/fiftyone_pose_importer/verification/cropper.py
@src/fiftyone_pose_importer/run_verify.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real crop materialization in cropper (including D-11 padded skeleton output)</name>
  <read_first>
@src/fiftyone_pose_importer/verification/cropper.py
@tests/phase6/test_cropper.py
  </read_first>
  <files>src/fiftyone_pose_importer/verification/cropper.py, tests/phase6/test_cropper.py</files>
  <action>Extend cropper with a crop materialization function that consumes CropPlan and source image, writes an output PNG, and returns the written path. For skeleton/keypoint annotations, materialize full padded canvas with out-of-image padded fill exactly per D-11 and fixed-pixel padding policy D-10; for non-skeleton annotations, materialize clipped crop per D-13. Keep existing `plan_crop` verdict semantics unchanged (no rework of verified D-01..D-03 logic).</action>
  <acceptance_criteria>Tests prove skeleton output dimensions equal padded canvas, out-of-image regions are physically present in the saved crop, and non-skeleton output remains clipped behavior.</acceptance_criteria>
  <verify>
    <automated>pytest -q tests/phase6/test_cropper.py::test_materialize_crop_skeleton_padded_canvas tests/phase6/test_cropper.py::test_materialize_crop_non_skeleton_clipped -x</automated>
  </verify>
  <done>Cropper now writes real crops to disk with D-11 behavior materialized for skeleton labels and existing crop planning behavior preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire run_verify to generate and report real crop artifact paths</name>
  <read_first>
@src/fiftyone_pose_importer/run_verify.py
@tests/phase6/test_run_verify.py
@src/fiftyone_pose_importer/datumaro_reader.py
  </read_first>
  <files>src/fiftyone_pose_importer/run_verify.py, tests/phase6/test_run_verify.py</files>
  <action>Update run pipeline so each annotation resolves source image path, materializes crop to the run directory (`run_dir/crops/...png`), and passes that real path into deterministic result/report payloads (per D-18). Resolve relative image paths against config directory and optional `image_dir` when present; guard path traversal and image decode/write errors by producing deterministic FAIL reasons instead of crashing (consistent with D-03/D-05). Do not modify deterministic rule categories, verdict aggregation, or report schema beyond replacing synthetic crop_path with real artifact path.</action>
  <acceptance_criteria>Integration tests confirm crop files are physically created for processed objects, summary/object records reference existing files, and run still exits successfully with deterministic FAIL rows when crop materialization for an object cannot be completed.</acceptance_criteria>
  <verify>
    <automated>pytest -q tests/phase6/test_run_verify.py::test_deterministic_only_pipeline_writes_real_crop_artifacts tests/phase6/test_run_verify.py::test_crop_path_in_summary_points_to_existing_file -x</automated>
  </verify>
  <done>Verification output now links to real generated crop artifacts, closing SC1/VER-01 and D-11 gap without changing already verified deterministic rule/report behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datumaro metadata → filesystem image read | Untrusted `image.path` crosses into local file access |
| Verification pipeline → run artifact writes | Untrusted identifiers become output filenames/paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-GC-01 | Tampering | image path resolution in run_verify | mitigate | Canonicalize/resolve paths; reject traversal escapes outside allowed roots before opening files |
| T-06-GC-02 | DoS | image decode/crop materialization | mitigate | Catch decode/write errors per object, emit deterministic FAIL reason, continue run per D-03/D-05 |
| T-06-GC-SC | Tampering | npm/pip/cargo installs | accept | No package install work in this gap closure plan |
</threat_model>

<verification>
Run:
- `pytest -q tests/phase6/test_cropper.py -x`
- `pytest -q tests/phase6/test_run_verify.py -x`
- `pytest -q tests/phase6 -x`
</verification>

<success_criteria>
- VERIFICATION.md blocker “real crop generation missing” is closed.
- D-11 is materially satisfied: skeleton out-of-bounds padding exists in saved crop outputs.
- Deterministic reports reference real crop files for processed objects.
- Previously verified deterministic rule/report behavior remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-deterministic-verification-core-reporting/06-05-SUMMARY.md` when done
</output>
